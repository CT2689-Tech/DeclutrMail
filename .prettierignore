node_modules
dist
build
.next
.turbo
coverage
storybook-static
.claude
pnpm-lock.yaml
*.min.js
*.min.css
docs/execution/Implementation-Plan.md

# Drizzle-generated migration metadata + SQL.
# meta/*.json must be byte-identical to drizzle-kit's output (used as the
# round-trip snapshot). The SQL files are also drizzle-emitted and should
# remain in the exact shape drizzle re-emits on next generate.
packages/db/migrations/**

# Curated policy / log documents — founder-edited, not auto-formatted.
# See CLAUDE.md §11 (LEARNINGS / MISTAKES / FOUNDER-FOLLOWUPS / IMPLEMENTATION-LOG lifecycle).
CLAUDE.md
LEARNINGS.md
MISTAKES.md
FOUNDER-FOLLOWUPS.md
IMPLEMENTATION-LOG.md
README.md
.github/pull_request_template.md
.claude/agents/**/*.md

# Recorded implementation-log state. Machine-written, and markdown
# emphasis rules would eat identifiers in evidence paths — prettier
# turned `__tests__` into `**tests**` here on 2026-08-19, which failed
# the evidence-file audit and demoted a Verified row.
.impl-log/

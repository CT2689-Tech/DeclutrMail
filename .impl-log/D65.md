# D65

<!-- Recorded state for this decision. Derived state (⬜/🔵 and the
     shipping PR) is recomputed by `pnpm generate-impl-log` and is
     NOT stored here. Delete this file to drop back to derived. -->

status: 🟢
verified-by: apps/web/src/features/brief/noise-archive.test.tsx ("checks every actionable Noise sender by default (D65)" + preview-before-mutation order per D226)
note: Was ⬜ Not started while the feature shipped — the per-sender checkboxes, the archive sheet and the "Archive N senders" bar all render on /brief. No merged PR carried a Closes D65 trailer, so the derivation had nothing to find, and the shipping PR is not recoverable from git (the file's first-add commit c80a21b is a 1,746-file history import, not a feature merge). Recorded here rather than guessing a number.

{
  "name": "@declutrmail/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install chromium",
    "typecheck": "tsc --noEmit",
    "build": "echo 'no build'"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "postgres": "^3.4.5"
  }
}

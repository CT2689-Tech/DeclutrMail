# True build status — D126 → D251

**Audited** 2026-08-11 · repo `main` @ `40e73a4f` · READ-ONLY (no edits, no dev server, no DB)
**Sources** `docs/execution/Implementation-Plan.md` incl. every `[GRILL2 PATCH]` / `[AUDIT PATCH]` / `[ADR-#### PATCH]` / `[REVERSAL]` / `[AMENDMENT]` block · `IMPLEMENTATION-LOG.md` · `CLAUDE.md` §2 · `docs/adr/` · live code.
**Coverage** D126–D235 plus the post-235 additions found in the plan: **D245, D246, D248, D249, D250, D251**. `D247` is a phantom — cited by merged PR #353, present in no plan section and no log row.

---

## 1. Headline

**Of the 48 ⬜ "Not started" rows in D126–D248, only 10 are genuinely unbuilt. 21 are finished, obsolete, or policy-only and are mislabelled outright; the remaining 17 are partially built.** The log's error runs both ways and the second direction is worse: **14 rows the log calls 🔵 or 🟢 are actually PARTIAL** (D126, D132, D134, D137, D150, D156, D159, D160, D167, D169, D183, D206, D220, D234), including four 🟢 rows — the log's strongest claim — every one of them overstating (D150 missing its trigram index, D156 missing the global IP ceiling, D159 with no server-side PostHog, D160 running 1 of 10 Playwright specs in CI). Because the table is derived purely from `Closes D###` trailers, an unbuilt decision and a shipped-without-a-trailer decision are indistinguishable, and a decision cited by a PR that implemented one clause of six reads as fully shipped. The genuinely-remaining launch-relevant work is small and concentrated: **bot protection (D176), a global rate-limit ceiling (D156), offline display (D171), the missing E2E CI coverage (D160), and the CASA-letter claim (D141) which is a founder ruling, not engineering.** Everything else that is user-visible and unbuilt is deliberately deferred (D190 Quiet preview, D233 offline draft intents — the latter already superseded for beta by D246).

---

## 2. Rows where the verdict differs from the log, or where real work remains

Effort = work remaining to finish. S = under an hour · M = half a day · L = more · `–` = nothing left.

### 2.1 Marketing / brand / content — D126–D149

| D#   | title (short)                        | log says  | actual                                                                                                                                                                                                                                                                                                                      | evidence (file:line)                                                                                                                                                               | effort         | user-visible? |
| ---- | ------------------------------------ | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------- |
| D126 | Retention 5-part package             | 🔵 (#497) | **PARTIAL** — P1 instrumentation partial (90-event taxonomy shipped; no cohort metrics, **zero alert thresholds**); P2 "queue running empty → weekly review mode" **NOT-BUILT**; P3 only the Day-7 lapse email of four exists; P4 pricing+cap+badge done, Day-60 annual-switch prompt missing; P5 no-lifetime **compliant** | `packages/shared/src/observability/events.ts:14`; `packages/workers/src/lapse-reengagement.worker.ts:25,166`; `packages/shared/src/entitlements/pricing.config.ts:132,156,177,182` | L              | yes           |
| D127 | Brand name                           | ⬜        | **DONE**                                                                                                                                                                                                                                                                                                                    | `apps/web/src/features/marketing/public-shell/public-shell.tsx:69`                                                                                                                 | –              | yes           |
| D129 | Hero tagline                         | ⬜        | **OBSOLETE** — D223 → `[REVERSAL 2026-08-02 on D223]` → D250. The D129 string exists only in the plan                                                                                                                                                                                                                       | `apps/web/src/features/marketing/landing/hero.tsx:34`; plan `:9689,:9729`                                                                                                          | –              | –             |
| D130 | Brand voice rules                    | ⬜        | **PARTIAL** — hook enforces the D209 subset (`AI-powered`, `supercharged`, `nuke/destroy/blast`, standalone `smart`/`intelligent`, `clean` as verb). Not enforced: no-exclamation-marks, no-emoji, `amazing`/`powerful`/`revolutionary`                                                                                     | `.claude/hooks/check-microcopy.sh:186,192,210`                                                                                                                                     | S              | partly        |
| D131 | Top nav 6-item flat                  | ⬜        | **PARTIAL (drift)** — real nav is Logo · How it works · **Demo** · Methodology · Compare · Pricing. **`Blog` is not in top nav** (demoted to footer); `Demo` took its slot                                                                                                                                                  | `apps/web/src/features/marketing/public-shell/public-shell.tsx:10-16,34`                                                                                                           | S              | yes           |
| D132 | 28-page launch IA                    | 🔵        | **PARTIAL — 27/28** — only `/post-oauth` absent (arguably superseded: callback is server-side + `/onboarding`). 7 extra pages beyond the IA                                                                                                                                                                                 | `apps/web/src/app/sitemap.ts:17-38`                                                                                                                                                | S              | yes           |
| D133 | Inbox Simulator **with real engine** | ⬜        | **PARTIAL — core demand unmet.** Reuses real production components, but verdicts are **hard-coded fixture strings, not `recommend()` output**; no shareable engine module exists. Also 10 rows not 15-20, and no "after 5 decisions → estimated impact" projection                                                          | `apps/web/src/features/marketing/inbox-simulator/inbox-simulator-screen.tsx:17,29,198`; `apps/web/src/features/triage/data.ts:200,211,239`                                         | M              | yes           |
| D134 | Landing 10-section structure         | 🔵        | **PARTIAL — 9 of 10.** Missing §7 comparison preview; **no `/compare` link anywhere on the landing page**                                                                                                                                                                                                                   | `apps/web/src/app/(marketing)/page.tsx:26-29,43-59`                                                                                                                                | S              | yes           |
| D135 | Hero animated preview card           | ⬜        | **DONE (tech deviation)** — CSS keyframes not Framer Motion; one-shot 8s timeline not a loop; reduced-motion honoured                                                                                                                                                                                                       | `apps/web/src/features/marketing/landing/hero.tsx:88-146`; `landing.css:360,424`                                                                                                   | –              | yes           |
| D136 | Social proof / no testimonials       | ⬜        | **PARTIAL** — "no testimonials" honoured (0 markup), `/changelog` covers build-in-public. **Founder bio + photo in methodology does not exist**                                                                                                                                                                             | `apps/web/src/app/(marketing)/page.tsx:28`                                                                                                                                         | S              | yes           |
| D137 | FAQ 10 questions                     | 🔵        | **PARTIAL — 7 of 10.** Missing: "Why pay $19/mo when Gmail is free?", "Will you ever sell my data?" (as a Q), "Does it work for work / non-Gmail accounts?"                                                                                                                                                                 | `apps/web/src/features/marketing/landing/faq.tsx:27-62`; `learn/faq-content.ts:7-137`                                                                                              | S              | yes           |
| D138 | Trust strip                          | ⬜        | **PARTIAL** — strip exists, 3 items; "Bodies read: 0 forever" correctly retired per D228. Missing "CASA Tier 2 verified" (deliberate) + "Read our methodology →" link                                                                                                                                                       | `apps/web/src/features/marketing/landing/hero.tsx:154-170`                                                                                                                         | S              | yes           |
| D139 | Methodology layered                  | ⬜        | **PARTIAL — 7 of 8 sections**; §8 Founder's note missing; layering (`<details>`) on only 2 of 7                                                                                                                                                                                                                             | `apps/web/src/app/(marketing)/methodology/page.tsx:29-218,42,66`                                                                                                                   | M              | yes           |
| D140 | 3 conceptual diagrams                | ⬜        | **DONE** — all three shipped as accessible `<figure>`s, plus a 4th                                                                                                                                                                                                                                                          | `apps/web/src/features/marketing/product-story/diagrams.tsx:175,222,245,295`                                                                                                       | –              | yes           |
| D141 | CASA letter PDF                      | ⬜        | **NOT-BUILT and effectively unbuildable as written.** No PDF exists; the codebase asserts no letter exists and a test **forbids** claiming one. The annual re-cert note IS shipped                                                                                                                                          | `apps/web/src/app/(marketing)/security/page.tsx:13-17,113`; `apps/web/src/app/llms-txt.test.ts:41`                                                                                 | founder ruling | yes           |
| D142 | Comparison tone balanced             | ⬜        | **DONE**                                                                                                                                                                                                                                                                                                                    | `apps/web/src/features/marketing/comparison/comparison-data.ts:1-25,148,157`                                                                                                       | –              | yes           |
| D143 | 10-row comparison table              | ⬜        | **PARTIAL — 8 rows.** No dedicated row for Activity audit log, Brief, or the money-back guarantee                                                                                                                                                                                                                           | `comparison-data.ts:164-244`                                                                                                                                                       | S              | yes           |
| D144 | "Choose competitor if…" callouts     | ⬜        | **DONE** — 5 planned slugs + `/vs/gmail`                                                                                                                                                                                                                                                                                    | `comparison-data.ts:148,278,408,539,674,808`                                                                                                                                       | –              | yes           |
| D145 | `/compare` index                     | ⬜        | **DONE**                                                                                                                                                                                                                                                                                                                    | `apps/web/src/app/(marketing)/compare/page.tsx:16`                                                                                                                                 | –              | yes           |
| D146 | Privacy + Terms                      | 🔵        | **DONE** (stored as TSX routes, not `/docs/legal/`)                                                                                                                                                                                                                                                                         | `apps/web/src/app/(marketing)/privacy/page.tsx:56-364`                                                                                                                             | –              | yes           |
| D149 | Launch strategy                      | ⬜        | **N/A — policy-only**, zero code citations                                                                                                                                                                                                                                                                                  | plan `:3825`                                                                                                                                                                       | –              | no            |

### 2.2 Infrastructure / API / DB / security — D150–D181, D193, D195

| D#   | title (short)                             | log says | actual                                                                                                                                                                                                                                                                                                 | evidence (file:line)                                                                                          | effort                            | user-visible? |
| ---- | ----------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------- |
| D150 | 12 launch indexes                         | 🟢       | **PARTIAL** — 77 named indexes incl. every composite + the partial index, but the **trigram/GIN index does not exist** (no `pg_trgm`, no GIN anywhere). Sender search is a leading-wildcard `ILIKE`                                                                                                    | `packages/db/src/schema/mail-messages.ts:165-205`; `apps/api/src/senders/senders.read-service.ts:264-266`     | S                                 | no (latency)  |
| D151 | Hybrid partitioning                       | ⬜       | **OBSOLETE** — reversed by D235 + `[GRILL2 PATCH on D150]`. Zero `PARTITION BY` DDL across 54 migrations                                                                                                                                                                                               | plan `:9146,:9239`                                                                                            | –                                 | no            |
| D153 | REST + Zod + **Swagger**                  | ⬜       | **PARTIAL** — REST + Zod + envelope shipped; **Swagger/OpenAPI entirely absent** (no `@nestjs/swagger`, no `SwaggerModule`, no `/api/docs`, no `openapi.*`)                                                                                                                                            | `packages/shared/src/contracts/envelope.ts:39`; absence repo-wide                                             | M                                 | no            |
| D154 | `/api/v1/` prefix                         | ⬜       | **NOT-BUILT** — shipped prefix is `/api`; no `enableVersioning`, no `@Version`. **Exactly one** of 34 controllers carries a `v1` segment — inconsistent drift, not versioning. _(Verified directly.)_                                                                                                  | `apps/api/src/main.ts:170`; `apps/api/src/sync/sync.controller.ts:52`                                         | M (+ founder: OAuth redirect URI) | no            |
| D155 | Cookies + CSRF + rotation                 | 🔵       | **DONE** — table, indexes, 3 cookies at spec shapes, CSRF guard, real rotation                                                                                                                                                                                                                         | `packages/db/src/schema/active-sessions.ts:39-71`; `apps/api/src/auth/session-cookies.ts:44-63`               | –                                 | yes           |
| D156 | Rate limiting + **global IP ceiling**     | 🟢       | **PARTIAL** — custom Redis/Lua bucket (not `@nestjs/throttler`), 116 annotated handlers, `Retry-After`, breach→security event. **No global IP ceiling**: the interceptor is opt-in, so an unannotated route passes straight through and new controllers default to unprotected. _(Verified directly.)_ | `apps/api/src/common/rate-limit/rate-limit.interceptor.ts:110-113`                                            | S                                 | no            |
| D157 | BullMQ on Upstash                         | 🟢       | **DONE (drift)** — 19 workers, `rediss://`, policy-driven retries, DLQ + sweeper. Not `@nestjs/bull`; roster differs from both plan lists                                                                                                                                                              | `apps/api/src/worker.ts:643-2081`; `packages/workers/src/queue.ts:78,143,226`                                 | –                                 | no            |
| D158 | Hosting stack                             | 🔵       | **DONE, amended** — Cloud SQL → Supabase per ADR-0022; max-instances 3/1 vs plan's 10/5 (deliberate dev sizing)                                                                                                                                                                                        | `.github/workflows/deploy-cloud-run.yml:269-272,331-337`; `docs/adr/0022-postgres-supabase-pre-launch.md:1-8` | –                                 | no            |
| D159 | Sentry + PostHog                          | 🟢       | **PARTIAL** — Sentry both sides ✓. **PostHog is client-only**: no server SDK in `apps/api` or `packages/workers`, so the plan's server metrics (queue depth, DLQ count, Gmail errors, sync lag, watch-renewal failures) never reach it. Perf traces deliberately off → no p95 source. No Grafana       | `apps/api/src/observability/sentry.ts:3-20`; `apps/web/package.json:19,22`                                    | M                                 | no            |
| D160 | CI/CD + required checks                   | 🟢       | **PARTIAL** — deploy + typecheck/lint/format/tests + Atlas gate ✓. **Missing: bundle-size budget, Lighthouse ≥90, and real E2E coverage** — the Playwright job runs only `e2e:a11y` (+billing), pinned to 1–2 of 10 specs                                                                              | `.github/workflows/ci.yml:366,468,475`; `packages/e2e/package.json:8`                                         | M                                 | no            |
| D161 | Dev-phase cost optimizations              | ⬜       | **OBSOLETE** — (a) superseded by ADR-0022 (Cloud SQL never provisioned); (b) explicitly superseded by D195                                                                                                                                                                                             | `docs/adr/0022-postgres-supabase-pre-launch.md:8`; plan `:4977`                                               | –                                 | no            |
| D162 | Resend + React-Email                      | 🔵       | **DONE** — 7 templates, port seam, send worker, svix-verified inbound + suppression                                                                                                                                                                                                                    | `apps/api/package.json:30,31,44`; `apps/api/src/notifications/email.service.ts:52`                            | –                                 | yes           |
| D163 | Web Push + VAPID                          | ⬜       | **OBSOLETE + NOT-BUILT** — explicitly superseded by D246; zero hits for `web-push`/`VAPID`/`pushManager`                                                                                                                                                                                               | plan `:9520`; `docs/execution/behavioral-activation-trust-d246.md:26-31`                                      | –                                 | no            |
| D164 | Mobile push deferred                      | ⬜       | **N/A** — policy, reaffirmed by D246                                                                                                                                                                                                                                                                   | `behavioral-activation-trust-d246.md:31`                                                                      | –                                 | no            |
| D165 | Per-category notification toggles         | 🔵       | **DONE for every category that exists** — 3 toggles + PATCH route + settings card + worker-side opt-out; the plan's 4th (browser push) is obsolete via D163/D246                                                                                                                                       | `packages/shared/src/contracts/email-prefs.ts:25-41`                                                          | –                                 | yes           |
| D172 | pnpm + **Turborepo**, 3 apps + 3 packages | ⬜       | **PARTIAL / structural drift** — **no Turborepo at all** (no `turbo.json`, no dep); orchestration is `pnpm -r --parallel`. 2 apps not 3 (worker ships from the API image); 6 packages not 3                                                                                                            | `package.json:12-27`; `turbo.json` absent                                                                     | – (accept) or M (adopt)           | no            |
| D174 | `.worktreeinclude`                        | ⬜       | **NOT-BUILT** — file absent; `wt-new.sh` only validates branch names. Largely moot: most of D174's list is git-tracked, and env-copying is handled by `sync-env-to-worktree.sh`                                                                                                                        | `scripts/wt-new.sh:40`; `scripts/sync-env-to-worktree.sh:17-18`                                               | S                                 | no            |
| D175 | Strict nonce CSP                          | 🔵       | **DONE, one self-flagged deviation** — `style-src 'self' 'unsafe-inline'` with a 12-line in-file rationale (nonces cannot authorize style _attributes_)                                                                                                                                                | `apps/web/src/middleware.ts:113,140,160,214`                                                                  | –                                 | no            |
| D176 | Cloudflare Turnstile                      | ⬜       | **NOT-BUILT** — zero code hits for `turnstile` (case-insensitive)                                                                                                                                                                                                                                      | absence + `docs/execution/launch-gap-audit-2026-06-09.md:165`                                                 | M (+ founder: CF keys)            | **yes**       |
| D177 | GCP Secret Manager                        | ⬜       | **PARTIAL** — Cloud Run mounts secrets via `--update-secrets`, SA bound, inventory runbook, preflight audit. **Vercel does not reference Secret Manager** (plain env vars, documented). Quarterly JWT rotation not automated                                                                           | `.github/workflows/deploy-cloud-run.yml:276,342`; `docs/runbooks/secrets-inventory.md:72-77`                  | S                                 | no            |
| D178 | Dependabot + Snyk                         | ⬜       | **PARTIAL** — Dependabot shipped (weekly, 3 ecosystems, grouped). **Snyk absent**, with the reason written into the file (needs account + token)                                                                                                                                                       | `.github/dependabot.yml:1,11-15`                                                                              | S + founder                       | no            |
| D179 | CORS                                      | 🔵       | **DONE, cosmetic deviations** — no explicit `methods` allowlist, no `maxAge`, `X-Correlation-ID` deliberately not in `allowedHeaders`                                                                                                                                                                  | `apps/api/src/main.ts:143,158-169`                                                                            | S                                 | no            |
| D180 | Webhook signature verification            | 🔵       | **DONE** — Paddle + Razorpay HMAC w/ `timingSafeEqual`, Resend svix, Gmail Pub/Sub full OIDC + dedup, banned-header regression test                                                                                                                                                                    | `apps/api/src/webhooks/oidc-verifier.ts:19-27,95-100`; `webhooks/__tests__/banned-header.spec.ts:54`          | –                                 | no            |
| D181 | Security events log                       | 🔵       | **PARTIAL** — table matches spec exactly, 24 event types actually written, admin UI shipped. **Missing: CSP violation reports** — no `report-to`/`report-uri` and no ingest endpoint (the middleware test _asserts_ their absence)                                                                     | `packages/db/src/schema/security-events.ts:33-73`; `apps/web/src/middleware.test.ts:98-99`                    | S                                 | admin only    |
| D193 | API `min_instances=1`                     | 🔵       | **DONE** (a July-2026 audit doc claiming 0 is stale)                                                                                                                                                                                                                                                   | `.github/workflows/deploy-cloud-run.yml:253,269-270`                                                          | –                                 | no (latency)  |
| D195 | Worker scaling decoupled from billing     | ⬜       | **DONE** — zero `gcloud`/`run services update`/`min-instances` strings in app code; worker min=1 is declarative; the billing webhook only verifies → dedupes → updates entitlements → acks                                                                                                             | `deploy-cloud-run.yml:331-332`; `apps/api/src/billing/billing-webhook.service.ts:174-199`                     | –                                 | no            |

### 2.3 Edge states / testing / process — D166–D171, D182–D192, D194

| D#   | title (short)                                | log says  | actual                                                                                                                                                                                                                                                                                                                           | evidence (file:line)                                                                                                                                     | effort                     | user-visible?          |
| ---- | -------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------- |
| D167 | 404/500 calm branded                         | 🟢        | **PARTIAL** — pages show Next's `error.digest`, **not** the D168 `DM-XXXXXX` id (`deriveDisplayId` has zero web consumers); no "Open Activity Log" CTA                                                                                                                                                                           | `apps/web/src/app/error.tsx:130,168`                                                                                                                     | S                          | yes                    |
| D169 | Three severity tiers                         | 🔵        | **PARTIAL** — 2 of 3 live; `silent_transient` is **declared and never assigned**; `BaseDeclutrWorker` has no severity concept, contradicting the contract doc                                                                                                                                                                    | `packages/shared/src/contracts/error-envelope.ts:28-34,43`                                                                                               | S                          | no                     |
| D170 | Three critical-trust scenarios               | ⬜        | **PARTIAL — 2 of 3.** (1) OAuth-revoked banner ✓ but action `status='paused'` **does not exist** (enum is `queued\|executing\|done\|failed`) and `OAUTH_REVOKED` is never thrown in `apps/api`; (2) sync-corruption banner + admin escalation **NOT handled**; (3) action-uncertainty verifier **DONE, stronger than specced**   | `packages/db/src/schema/action-jobs.ts:110`; `apps/web/src/features/sync/sync-error-banner.tsx:41-44`; `apps/api/src/worker.ts:930-947`                  | M                          | yes                    |
| D171 | Offline / poor-network UX                    | ⬜        | **NOT-BUILT; queue half OBSOLETE** — D246.1 explicitly supersedes the queued-action portion. Remainder unbuilt: zero `navigator.onLine`/`onlineManager` in app code; the `offline` edge state is declared with all 19 screen rows `required:false` / `todo`                                                                      | `behavioral-activation-trust-d246.md:27-31`; `packages/shared/src/edge-states/inventory.ts:57,208-211`                                                   | M                          | yes                    |
| D182 | Vitest + **testcontainers** + Playwright     | ⬜        | **PARTIAL — 2 of 3.** **testcontainers is not installed anywhere.** Substitutes: PGlite + one GH `postgres:16` service. This gap has already cost once — `MISTAKES.md:1620` records a raw-SQL `Date`-param defect PGlite passes and real postgres.js rejects                                                                     | `.github/workflows/ci.yml:185-212`; absence of `testcontainers`                                                                                          | M                          | no                     |
| D183 | MockGmailProvider + fixtures + staging Gmail | 🔵        | **PARTIAL — 1 of 3, reshaped.** No `MockGmailProvider` symbol exists; what ships is the D201 port + per-test inline fakes. No recorded fixtures (no nock/polly/msw). No staging real-Gmail run                                                                                                                                   | `packages/workers/src/ports.ts`; `packages/e2e/global-setup.ts:25`                                                                                       | M                          | no                     |
| D184 | 70% coverage floor                           | ⬜        | **NOT-BUILT (aspirational)** — no `thresholds` key in any vitest config; CI runs zero `--coverage`. Whether the floor would _pass_ if wired is **UNKNOWN**                                                                                                                                                                       | `packages/shared/vitest.config.ts`; `.github/workflows/ci.yml:49` (comment only)                                                                         | S to wire                  | no                     |
| D185 | Skip visual regression                       | ⬜        | **DONE-by-omission / N/A** — no Chromatic, no Percy                                                                                                                                                                                                                                                                              | dep + word grep                                                                                                                                          | –                          | no                     |
| D186 | CLAUDE.md content deferred                   | ⬜        | **DONE / OBSOLETE** — file exists and exceeds the draft skeleton                                                                                                                                                                                                                                                                 | `CLAUDE.md`                                                                                                                                              | –                          | no                     |
| D187 | Anti-redesign scope freeze                   | ⬜        | **NOT-BUILT — the enforcement is a no-op.** All three legs missing: no D187/waiver line in the PR template, `require-pr-template.sh` enforces only the branch-name regex, and zero `redesign`/`label` matches in `.github/workflows/`. Yet `CLAUDE.md:269` and a launch-audit doc both assign work to "the `redesign` label"     | `.github/pull_request_template.md`; `.claude/hooks/require-pr-template.sh:6-16,51`                                                                       | S                          | no                     |
| D188 | Launch feature flags                         | ⬜        | **NOT-BUILT as specced; a different system shipped.** None of the six named flags exist; ADR-0025's `FLAG_MANIFEST` solves the need more simply but **never mentions D188**, so the plan still carries a live decision (DB table, workspace overrides, rollout %, `/admin/feature-flags`) the code silently answered differently | `packages/shared/src/flags/manifest.ts`; `docs/adr/0025-feature-flag-manifest.md`                                                                        | L as written / S to ratify | no                     |
| D189 | Weekly Value Receipt                         | 🔵 (#497) | **DONE (as amended 2026-08-02)** — worker (Sun ≥18:00 local, 7d window, empty-suppression tested), email template, Plus/Pro gate, default-off pref, **and** the in-app card mounted on Activity. Email is postal-blocked                                                                                                         | `packages/workers/src/weekly-value-receipt.worker.ts:101-129`; `apps/web/src/features/activity/weekly-review-card.tsx` mounted `activity-screen.tsx:326` | –                          | yes                    |
| D190 | Quiet Mode Preview Mode                      | ⬜        | **NOT-BUILT (premise not shipped)** — the Quiet screen states in-code that D190 preview is out of scope; no `quiet_preview_log` table; `preview_mode_until` exists only in a doc comment with no typed field and no reader                                                                                                       | `apps/web/src/features/quiet/quiet-screen.tsx:26-27`; `packages/db/src/schema/mailbox-accounts.ts:26`                                                    | L                          | yes (when Quiet ships) |
| D191 | Sync gate kill metric                        | ⬜        | **PARTIAL** — `sync_started`/`sync_completed` emitted; **no `first_action_taken` event at all**, no `oauth_completed`, no tiered-gate path, and the 65% threshold is recorded nowhere in code or docs                                                                                                                            | `packages/shared/src/observability/events.ts:24,181`; `apps/web/src/features/sync/use-sync-funnel.ts:58`                                                 | M                          | no                     |
| D192 | Custom rule builder deferred                 | ⬜        | **OBSOLETE** — reversed by D196, re-refined by D197 + D234; those govern today and are built                                                                                                                                                                                                                                     | plan `:5164,:5308,:9259-9263`                                                                                                                            | –                          | no                     |
| D194 | Screener marketing-copy rule                 | ⬜        | **DONE — via a different, stronger mechanism.** The patch's named mechanism does not exist (`check-microcopy.sh` parses no CLI args, so `--rule=screener` is fiction). Real control is a CI-run test with a `FORBIDDEN` regex + positive controls                                                                                | `apps/web/src/features/marketing/screener-truth.test.ts:44,127-143`; `.claude/hooks/check-microcopy.sh:213-246`                                          | –                          | no                     |

### 2.4 Architecture / UI constitution / guardrails — D196–D235

| D#                               | title (short)                             | log says  | actual                                                                                                                                                                                                                                                                                                                                  | evidence (file:line)                                                                                                                              | effort      | user-visible? |
| -------------------------------- | ----------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------- |
| D206                             | Per-layer test templates + harnesses      | 🔵        | **PARTIAL** — the D206 deliverable `packages/shared/testing/` (templates, factories, worker-lifecycle harness, TanStack mutation harness, `create-test-db`, msw-handlers, unit-of-work harness) **does not exist**; no MSW anywhere. What shipped is the dev test-login + Playwright global-setup + seed helpers                        | plan `:7562-7663`; `packages/shared/src/` has no `testing/`; `apps/api/src/auth/dev-auth.controller.ts:11`                                        | M           | no            |
| D208                             | Preview mandatory                         | 🔵        | **DONE** — and the log's 2026-07-29 "cited evidence file no longer exists" note is **STALE**: the file is back on `main` and tests the mandate                                                                                                                                                                                          | `apps/web/src/features/triage/action-sheet.test.tsx:32,88,105`                                                                                    | –           | yes           |
| D213                             | Motion design discipline                  | ⬜        | **PARTIAL** — reduced-motion honoured everywhere, but the decision's actual artifact (duration tokens 150/250/400 ms in `packages/shared/tokens/motion.ts`) **was never created**. No `motion.ts`, no `--duration-*` properties                                                                                                         | `packages/shared/src/styles/tokens.css:335,384-392`; `find packages/shared -name "motion*"` → empty                                               | S           | no            |
| D214                             | Home strip atop Triage                    | ⬜        | **DONE** — `TodayStrip` built, mounted, story-covered, hides hollow zeros; no `/home` route                                                                                                                                                                                                                                             | `apps/web/src/features/triage/today-strip.tsx`; mounted `triage-screen.tsx:55,980`                                                                | –           | yes           |
| D215                             | Senders owns Search + Protected filter    | ⬜        | **DONE** — typeahead `q`, `isProtected`, URL-backed scope. `/settings/senders` is the standing-policies view, not a second directory                                                                                                                                                                                                    | `apps/web/src/features/senders/use-compose-state.ts:1-30`; `senders-screen.tsx:225,240`                                                           | –           | yes           |
| D220                             | Component naming inventory                | 🔵        | **PARTIAL — inventory has drifted hard.** 7 of the 10 locked names never shipped (`PageShell`, `PageHeader`, `UndoBanner`, `MetricCard`, `ActionPill`, `InsightBadge`, `TrustBadge`, `DataStorageCard`); 19 components sit in shared vs the patched count of 9 + 3 CLAUDE.md amendments. The `DangerZoneCard` demotion **is** compliant | plan `:8356-8371`; `[AUDIT PATCH on D220]` `:8799-8808`; `ls packages/shared/src/components/`                                                     | S (doc)     | no            |
| D221                             | Decision Queue framing                    | ⬜        | **PARTIAL** — "decisions" **is** the shipped count noun, but D221's stated enforcement was never built: `check-microcopy.sh` covers D227 verbs + D228 badge only; no rule rejects "senders"/"emails" as the Triage count noun                                                                                                           | `apps/web/src/features/triage/today-strip.tsx:18,118`; `.claude/hooks/check-microcopy.sh:1-17`                                                    | S           | partly        |
| D222                             | Category prediction REJECTED              | ⬜        | **DONE-as-enforced** — the hook is non-trivial (2 live regex families, with a documented removal of a third that false-positived). Not a blind guard                                                                                                                                                                                    | `.claude/hooks/block-category-prediction.sh:41,49`                                                                                                | –           | no            |
| D223                             | Landing headline                          | 🔵        | **OBSOLETE** — reversed by D250                                                                                                                                                                                                                                                                                                         | plan `:9689,:9729`                                                                                                                                | –           | –             |
| D226                             | Action lifecycle ordering                 | 🔵        | **DONE** — same stale-evidence note as D208 should be cleared                                                                                                                                                                                                                                                                           | `apps/web/src/features/triage/action-sheet.test.tsx:32-33,139-152,175-177`                                                                        | –           | yes           |
| D231                             | `GmailOpenLinkService`                    | ⬜        | **DONE** — service with `buildOpenLink` / `buildFromSearchLink` / `buildActionScopeSearchLink` / compose, `authuser=` (not `/u/0`), the D231 flag, tests, and it is the single construction point across 4 features                                                                                                                     | `apps/web/src/lib/gmail/open-link.ts:1-56`; flag `packages/shared/src/flags/manifest.ts:48-49`                                                    | –           | yes           |
| D232                             | Deletion respects undo windows            | 🔵        | **DONE** — `max(now+7d, latest_undo_expires_at)` + the literal `DELETE AND WAIVE UNDO` waiver, spec-tested                                                                                                                                                                                                                              | `apps/api/src/account/deletion.service.ts:42-46`; `deletion.service.spec.ts:219-232`                                                              | –           | yes           |
| D233                             | Offline actions = draft intents           | ⬜        | **NOT-BUILT, guardrail not violated.** No offline queue exists at all, so nothing can auto-replay — the invariant is satisfied **vacuously by absence**, not by an implemented review gate. D246 additionally _rejects_ browser-persisted offline mail-changing actions for beta                                                        | absence of `draftIntent`/`navigator.onLine`; `edge-states/inventory.ts:57-58`; D246 plan `:9520-9522`                                             | L if wanted | yes if built  |
| D234                             | Custom-rule API rejects `is_preset=false` | 🟢        | **PARTIAL / satisfied-by-absence** — see §4                                                                                                                                                                                                                                                                                             | `apps/api/src/autopilot/autopilot.controller.ts` (no create route)                                                                                | S           | no            |
| D235                             | Partitioning deferred                     | ⬜        | **DONE on the deferral, NOT-BUILT on the monitoring** — see §4                                                                                                                                                                                                                                                                          | see §4                                                                                                                                            | M           | no            |
| D248                             | Bulk unsubscribe                          | 🔵 (#500) | **DONE (build) — never executed live** — see §4                                                                                                                                                                                                                                                                                         | see §4                                                                                                                                            | –           | yes           |
| D245 / D246 / D249 / D250 / D251 | post-235 additions                        | 🔵        | **DONE-ongoing** — all five have live code                                                                                                                                                                                                                                                                                              | `apps/web/src/features/screener/upsell.tsx` (D251); `apps/api/src/activity/activity.read-service.ts` (D246); plan `:9443,:9508,:9634,:9729,:9789` | –           | yes           |
| D247                             | _(phantom)_                               | no row    | **N/A** — cited by merged PR #353, in no plan section                                                                                                                                                                                                                                                                                   | plan grep → 0 section hits                                                                                                                        | –           | –             |

---

## 3. Remaining work a user would actually notice, ordered by product impact

| #   | Work                                                                                                         | Why it matters                                                                                                                                                                                              | D                   | Effort           |
| --- | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ---------------- |
| 1   | **Run the first real bulk one-click unsubscribe**                                                            | The whole path is built and tested; nothing has ever been sent. Highest-risk unexercised destructive path before launch. Smoke, not build                                                                   | D248                | S (a run)        |
| 2   | **Bot protection on signup / OAuth init**                                                                    | The only _user-facing_ NOT-BUILT security item. Signup is currently unprotected                                                                                                                             | D176                | M + founder keys |
| 3   | **Global IP rate-limit ceiling**                                                                             | The interceptor is opt-in, so every unannotated route is unlimited and every new controller defaults to unprotected. Availability/abuse exposure, and it silently worsens as the API grows                  | D156                | S                |
| 4   | **Run the other 9 Playwright specs in CI**                                                                   | 9 of 10 E2E specs never execute. This is the class of gap that produced the founder's structural-green-but-broken-live sessions                                                                             | D160                | M                |
| 5   | **Inbox Simulator uses fixture verdicts, not the real engine**                                               | The marketing page's whole claim is "real engine". Today it is hard-coded strings — a truth problem on a public surface                                                                                     | D133                | M                |
| 6   | **Decide the CASA-letter claim**                                                                             | The plan promises a downloadable letter; the artifact does not exist and the codebase actively forbids implying one. Founder ruling, not engineering                                                        | D141                | ruling           |
| 7   | **Offline display**                                                                                          | `offline` is a declared edge state that is `todo` on all 19 surfaces. D246 already killed the replay queue, so the honest scope is a truthful banner — or flip all 19 rows to `n/a` and retire D171         | D171 (+D233)        | M                |
| 8   | **Landing gaps: no `/compare` link, 3 unwritten FAQs, missing trust-strip methodology link, no founder bio** | Small, cheap conversion-surface holes on the highest-traffic page                                                                                                                                           | D134/D137/D138/D136 | S each           |
| 9   | **Two shipped emails cannot send**                                                                           | `lapse-reengagement` (D126 P3) and `weekly-value-receipt` (D189) are both `COMMERCIAL_KIND` and `BUSINESS_POSTAL_ADDRESS = []` blocks every commercial send. Founder action (register an address), not code | D126/D189           | founder          |
| 10  | **D68 gate-card wording**                                                                                    | The paid-conversion surface ships wording that contradicts the plan; the plan promises something D226 forbids                                                                                               | D68                 | S (plan patch)   |
| 11  | **Sender search has no trigram index**                                                                       | Leading-wildcard `ILIKE` will degrade first and is the query D235's p95 trigger watches                                                                                                                     | D150                | S                |
| 12  | **Nothing will ever tell you a D235 threshold fired**                                                        | Deferral is durable; the alarm was never built                                                                                                                                                              | D235                | M                |

---

## 4. The five special-attention answers

### D68 — the "one-click archive" promise

**Exact plan wording**, `docs/execution/Implementation-Plan.md:2126-2144`, inside the D68 placeholder-card code block — the offending line is **`:2139`**:

```
### D68 — Free/Plus tier preview: **Placeholder + upgrade CTA**

Free and Plus users visiting `/brief` see a placeholder card:

⭐ Your Morning Brief
───────────────────────

A daily summary of yesterday's email,
written in plain English:

  REPLY  — what actually needs you
  FYI    — facts to know
  NOISE  — one-click archive          ← :2139
```

**The gate itself is DONE.** `TierGate` renders title + pitch + bullets + manifest-priced CTA + the D121 money-back note, short-circuits the under-tier fetch, and has tests and stories (`apps/web/src/features/billing/tier-gate.tsx:24-70`).

**The drift is already shipped and already logged.** `apps/web/src/app/(app)/brief/page.tsx:37` ships `'NOISE — archive the whole pile in one confirmed action'`, and lines 11-19 of that file state why: D226's preview is non-skippable, so "one-click" is not a gap D65 left open — it is a claim the architecture forbids any version of this feature from satisfying. Recorded at `FOUNDER-FOLLOWUPS.md:29-49` (Open), naming both gate sites (`brief/page.tsx` and `features/billing/tier-gate.stories.tsx:66`).

**Founder decision required:** add `[PATCH on D68]` replacing line 2139 with the shipped sentence, or specify different wording. Agents do not edit the plan. Until then a future session can "restore" the retired wording in good faith.

**Also:** `apps/web/src/features/brief/brief-screen.tsx:72-76` still carries a stale comment claiming the D68 gate is "NOT YET WIRED" and that no tier column exists. It is wired one layer up, at the route.

### D248 — bulk unsubscribe: built vs remaining

**Built — server-side fan-out is real, not a stub:**

| Leg                                                                                | Evidence                                                                                                                                                                                    |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Multi-sender execution                                                             | `apps/api/src/actions/actions.service.ts:884,2441,2581-2582`                                                                                                                                |
| Single-sender route stays single                                                   | `apps/api/src/actions/actions.controller.ts:110-123` — an `unsubscribe` primary on the composite route is rejected: "Unsubscribe one sender at a time through the unsubscribe-intent route" |
| Four-state partition (`one_click`/`mailto`/`none`/`NULL`=unknown), never collapsed | `features/senders/confirm-action-modal.tsx:307,842`; `lib/api/actions.ts:394`; `features/triage/data.ts:42,87,543`; `features/screener/screener-row.tsx:218`                                |
| Preview is the reversal point, no undo offered                                     | `features/senders/unsub-batch-receipt.tsx:61,97`                                                                                                                                            |
| Three terminal outcomes carried separately; "request accepted" not "unsubscribed"  | `lib/api/actions.ts:600,624`; `unsub-batch-receipt.stories.tsx:31,44`                                                                                                                       |
| Worker                                                                             | `packages/workers/src/unsub-execution.worker.ts` (+ `.test.ts`)                                                                                                                             |
| Screen wiring                                                                      | `features/senders/senders-screen.tsx:482,820,917,1276,1537,1793`                                                                                                                            |
| Tests + stories                                                                    | `confirm-action-modal.test.tsx:1179-1180`; `senders-screen.test.tsx:1590,1722`                                                                                                              |
| Post-merge fix already landed                                                      | PR #500 `fix(actions): report unknown unsubscribe state (D248)`                                                                                                                             |

**Remaining: nothing structural — one execution.** Nothing in the tree records a live bulk run; the deliberate never-run status is the outstanding item. (The log row flipped to 🔵 on PR #500 and is now correct as a _build_ claim — but 🔵 means "shipped, not yet verified", and the verification here is a live run, not a test.)

Two contract claims worth confirming during that first run: (a) the batch control genuinely does not offer itself when a selection contains zero `one_click` senders, and (b) an `unconfirmed` outcome survives a mixed run without rounding into accepted or failed.

### D65 — merged PR #498 vs the decision

**Matches:** per-sender checkboxes always visible and default-checked (`use-noise-archive.ts:202`); CTA recomputes on uncheck; confirm goes through the D226 mandatory preview; Protected senders excluded, shown as excluded rather than silently dropped, and re-dropped server-side if protection flips mid-flight; undo via the global tray; D69 respected (an archived row marks Done without recomputing the frozen snapshot).

**Three deliberate divergences, all documented in code:**

| D65 says                                                        | #498 ships                                                                                     | why                                                                                                                                                                                                                                                                                        |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| CTA reads "Archive X senders / Y messages"                      | CTA names **only the sender count**                                                            | the client cannot know the live message count; the only truthful one comes from the server and arrives in the preview one click later (`brief-screen.tsx:471-478`)                                                                                                                         |
| archives "the message IDs from those senders' yesterday emails" | archives each selected sender's mail **in the inbox now**, resolved by the worker at execution | the shipped action wire addresses senders, not frozen message-id sets — the `messages` selector was removed from the request schema 2026-07-27, so a yesterday-only archive is **not expressible today**. Every string on the surface names the wider scope (`use-noise-archive.ts:18-26`) |
| "7-day undo per D58"                                            | undo via the global tray, window per tier                                                      | consistent with every other action                                                                                                                                                                                                                                                         |

**One genuine gap:** D65's mobile clause — _"tap any part of a row to toggle, swipe-left also deselects"_ — **swipe is not implemented in Brief.** The repo's only swipe support is `apps/web/src/features/triage/use-swipe-verb.ts`. Tap-to-toggle works. **S.**

**One deliberate omission:** no PostHog event fires from this surface — `bulk_action_taken` has a closed `source` union with no Brief member, and the session refused to mislabel the event under another screen (`use-noise-archive.ts:66-73`). One-line union addition, **S**, but Brief bulk archives are invisible in analytics today.

### D234 — does the `is_preset=false` rejection actually exist?

**No — not as specified. It is satisfied by absence.**

- `AutopilotController` exposes `@Get('rules')`, `@Get('rules/:id')`, `@Get('rules/:id/matches')`, `@Patch('rules/:id')`, `@Post('pause-all')`, `@Post('pattern-suggestion/…')`, `@Post('matches/:matchId/dismiss')`, `@Post('matches/approve')`, `@Post('rules/:id/approve-all')`, `@Post('rules/:id/preview')`. **There is no rule-create route.**
- `grep custom_rules_not_available` across `apps/`, `packages/`, `docs/` returns **plan hits only**. The error code D234 mandates exists nowhere in code and no test asserts a 403.
- What does exist: reads and PATCH pin `is_preset = true` in the WHERE clause, so a custom-rule id is treated as not-found → 404 (`autopilot.read-service.ts:437-441,497`; `autopilot.controller.ts:141-146`; spec `autopilot.read-service.spec.ts:455`).
- Schema readiness is real: CHECK `automation_rules_preset_key_consistent` enforces `is_preset = true` IFF `preset_key IS NOT NULL` (`packages/db/src/schema/automation-rules.ts:80-82,203-209`; `migrations/0009_autopilot_rules.sql:10-15,53-55`).
- The flag D234 names for the V2.1 unlock (`custom_autopilot_builder_enabled`) and the `custom_autopilot_allowlist` table **do not exist**. The manifest holds only `darkMode`, `senderPeek`, `infiniteScroll`, `gmailDeeplinkSearchFallback` (`packages/shared/src/flags/manifest.ts:25-49`).

**Why it matters:** today nothing can create a custom rule over HTTP, so the _outcome_ holds. But D234 exists precisely because "an agent will faithfully implement `POST /rules` accepting arbitrary rule shapes." Nothing in the repo would stop that — no failing test, no gate, no error code to reach for. The invariant rests on a route nobody has written yet. **S to close:** add the create route with its gate, or add a contract test asserting the rejection.

### D235 — was partitioning added, and is the deferral durable?

**No partitioning was added.** Confirmed by two differently-shaped searches: `grep -ri partition` over `packages/db/src` + all 54 migrations returns **zero table-partitioning DDL** — the only hits are SQL window functions (`row_number() OVER (PARTITION BY …)`, `apps/api/src/actions/actions.service.ts:326-329`), an undo-scope comment (`migrations/0050_action_jobs_reach.rollback:5,24`), and ADR prose. `grep -rn D235` produces no schema or migration hit.

**The thresholds ARE durably documented, and one location actively enforces:**

| Where                                                    | What                                                                                                                    |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `.claude/agents/schema-migration-reviewer.md:91-93,165`  | **Enforcing** — "Check D — Partitioning awareness (D235)"; blocks any PR adding `PARTITION BY` before the triggers fire |
| `CLAUDE.md:152` (§2.6)                                   | the three thresholds verbatim                                                                                           |
| `docs/adr/0022-postgres-supabase-pre-launch.md:70-79`    | reuses the same 25M-row / 150ms numbers as the Supabase→Cloud SQL migration triggers                                    |
| `docs/adr/0014-senders-total-received-counter.md:19,121` | the p95 ceiling used as a live design constraint                                                                        |
| `docs/services.md:97`                                    | D-reference row                                                                                                         |

**Missing: the measurement.** D235's "how to apply" asked for dashboard panels (row count, per-mailbox percentile, sender-detail p95) plus alerts at 80% of each threshold. No watchdog reads those — `check-sync-stuck.sh`, `infra-snapshot.sh`, and `vendor-limits-watchdog.yml` all avoid those tables. **The thresholds are written down but nothing will ever tell you they fired.** Compounding it, D159 has no server-side PostHog and Sentry perf traces are deliberately off, so there is no p95 source to alert on even if a watchdog were written. **M.**

**Stale doc:** `docs/adr/0001-drizzle-orm.md:13,85,110` still lists D151 partitioning as planned, with no D235 reversal note.

---

## 5. Guardrails whose documented enforcement does not match reality

This is the BLIND-GUARD subform of the founder's UI-truth bug class, at the process layer. Every row below is a guard that reads as real in `CLAUDE.md` or the plan and checks nothing, or checks against something that does not exist.

| Guardrail                       | Documented as                                                                                                    | Reality                                                                                                                                                                        |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **D187 scope freeze**           | `CLAUDE.md:269` + a launch-audit doc assign work to "the `redesign` label"                                       | **no-op** — no PR-template line, `require-pr-template.sh` checks only the branch-name regex, zero `redesign` matches in `.github/workflows/`                                   |
| **D213 motion**                 | plan: "Architecture Guardian flags any duration outside the token set"; `design-system-agent.md:196-200` Check I | **the token set does not exist** — the check can only pass vacuously                                                                                                           |
| **D221 count noun**             | plan: "`check-microcopy.sh` rejects Triage strings using 'senders'/'emails' as the count noun"                   | **no such rule** in the hook                                                                                                                                                   |
| **D194 / D227 `--rule=` flags** | plan D194 patch: `--rule=screener`; `CLAUDE.md` §2.2: `--rule=canonical-verbs`                                   | **the script parses no CLI args at all** (`.claude/settings.json:103` invokes it bare). The rules themselves are real and unconditional; only the invocation string is fiction |
| **D226 preview hook**           | `CLAUDE.md` §2.3: "Enforced by `require-preview-before-mutation.sh` + architecture-guardian"                     | the hook is a **soft warning that always exits 0** (`:13`). Behaviour is correct in code; only the enforcement claim is overstated. The real gate is `design-system-agent`     |
| **D222 prediction ban**         | `CLAUDE.md` §2.4                                                                                                 | **accurate** — non-trivial, two live regex families, with a documented rejection of a third that false-positived                                                               |
| **D188 vs ADR-0025**            | plan carries DB-table flags, workspace overrides, rollout %, `/admin/feature-flags`                              | ADR-0025 shipped a simpler manifest and **never mentions D188**, so the plan still carries a live decision the code silently answered differently                              |

---

## 6. Searches I ran / what I could not check

**Plan coverage.** Enumerated every `### D###` heading and every amendment block (`grep -n '^### \['`): 9 AUDIT patches, 14 GRILL2 patches, 5 ADR patches, 2 REVERSALs (D223→D250; D77), 1 AMENDMENT (D189), 1 CODEX patch (D126 inline `:3352`). Every D in range was grepped by bare number across the whole plan, not only at its heading. **No patch block targets D126–D149 directly.**

**Code searches — the anti-blind-guard rule was applied throughout: every "absent" claim rests on at least two differently-shaped searches (feature noun AND the literal `D###` token), with `node_modules`, `storybook-static`, `.next`, `dist`, and `.claude/worktrees/**` excluded** (that last holds stale full repo copies that produce false positives).

Representative patterns: `PARTITION BY|partitionBy|pg_partman` · `swagger|SwaggerModule|@anatine|openapi|api/docs` · `enableVersioning|@Version(|api/v1|setGlobalPrefix` + full `@Controller(` enumeration · `@nestjs/throttler|Retry-After|@RateLimit(` + read of `intercept()` · `web-push|VAPID|pushManager|serviceWorker` · `turbo.json|turbo` · `worktreeinclude` · `turnstile|cf-turnstile` · `secretmanager|--set-secrets|--update-secrets` · `snyk` · `csp-report|report-to|report-uri` · `min.?instances|gcloud|run services update` · `pg_trgm|gin_trgm_ops|USING gin|CREATE EXTENSION` (all 54 migrations) · `testcontainers|pglite|nock|@pollyjs|msw` · `thresholds|coverage|--coverage` · `chromatic|percy` · `redesign|label` · the six D188 flag names · `weekly_value_receipt|value receipt` · `preview_mode_until|quiet_preview_log` · `first_action_taken|oauth_completed|tiered_sync_gate` · `navigator.onLine|useOnline|onlineManager|draft.?intent` · `silent_transient|critical_trust|deriveDisplayId|OAUTH_REVOKED|'paused'` · `motion|prefers-reduced-motion|--duration` · `TodayStrip|useTodaySummary` · `DangerZoneCard` · `GmailOpenLinkService|rfc822msgid|mail.google.com` · `latest_undo_expires_at|DELETE AND WAIVE UNDO` · `is_preset|custom_rules_not_available|custom_autopilot_builder_enabled` · `unsubscribe-intent|multi-sender` · `swipe` · `TierGate` · `lifetime|testimonial|founder|CASA` + repo-wide `*.pdf` find · `recommend(|recommendVerdict` · full `find apps/web/src/app -name page.tsx` route inventory.

**Independently re-verified** (not taken on a subagent's word): D154's `setGlobalPrefix('api')` at `apps/api/src/main.ts:170` and the single `@Controller('v1/sync')`; D156's opt-in pass-through at `rate-limit.interceptor.ts:110-113`.

**Could not check:**

- **Runtime behaviour.** Read-only mandate — no dev server, no DB, no smoke. Every verdict is static: code present, coherent, tested. **A green static read is not a smoke** (CLAUDE.md §8). Specifically unexercised: D248's first live bulk run, D65's real archive against Gmail, D232's purge sweep firing, D170's OAuth-revoked path.
- **Vendor-console state.** PostHog cohorts / retention insights / alert subscriptions (D126 P1, D191), Sentry alert rules, GitHub branch-protection required checks, live Cloud Run revision env (I read the declarative workflow, not the deployed service), whether Vercel env vars are populated.
- **Actual coverage percentages** for `packages/shared` / `packages/db` — needs a `vitest --coverage` run, so whether D184's 70% floor would _pass_ if wired is **UNKNOWN**.
- **External/DNS:** the live `.ai → .com` 301 behaviour, social handle reservations (D128).
- **Rendered visual fidelity** of the D135 hero animation and D140 diagrams.

**Log baseline.** All "log says" values are read from `IMPLEMENTATION-LOG.md` at `main` @ `40e73a4f` — 48 ⬜ / 44 🔵 / 21 🟢 across the 113 rows in D126–D248. If you compare against an older checkout the numbers will differ: PRs #497 and #500 recently flipped D126, D189, and D248 out of ⬜.

**One mis-tag found, worth knowing because it pollutes D-number greps:** `apps/web/src/app/not-found.tsx:14,39` and `not-found.test.tsx:44,55` cite "D140" for audience-aware 404 routing. D140 is the methodology-diagrams decision.

# Launch Buildout Plan — 2026-06-11

> **Method.** 13 parallel read-only verifiers + 1 completeness critic (15 agents,
> 522 tool calls) re-checked every row of `launch-gap-audit-2026-06-09.md` against
> code as of `c8dc8f6` (post PR #190). Every status below carries file:line
> evidence (full record in the Phase-0 workflow output). Supersedes the 06-09
> audit's wave plan. Pairs with `buildout-prerequisites-2026-06-11.md` (founder
> ledger — OAuth/CASA, Resend, Paddle/Razorpay KYC, prod env, webhook, vendor
> caps, Sentry all DONE; tiers locked).

---

## 1. What is genuinely BUILT (no work; audit rows closed)

| Surface                                                                                                                                                                   | Evidence anchor                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Triage K/A/U/L verbs → `POST /api/actions` with D226 preview (sheet + inline), server-confirmed row removal                                                               | `triage-screen.tsx:262-398`, `actions.controller.ts:138-187`         |
| Undo tray on triage + `Z` shortcut + 410-expired handling                                                                                                                 | `triage/page.tsx:50`, `triage-undo-tray.tsx`                         |
| Triage error state (explicit retry, no 4xx auto-retry)                                                                                                                    | `compose-state.ts:27-32`                                             |
| Keep/VIP/Protect policy writes (`PATCH /api/senders/:id/policy` + outbox projections)                                                                                     | `senders.controller.ts:359-383`, `senders-policy.service.ts:108-176` |
| Multi-sender bulk A/L/D with real previews, real receipts, partial-failure naming                                                                                         | `senders-screen.tsx:875-936`, `actions.service.ts:690`               |
| Unsubscribe one-click RFC 8058 execution (SSRF-hardened, DNS-pinned, honest outcomes), registered in worker.ts, wired from all 3 surfaces; mailto = manual compose (D230) | `unsub-execution.worker.ts`, `worker.ts:769`                         |
| Gmail Pub/Sub webhook D229 OIDC 8-step + dedup + monotonic cursor + post-commit enqueue                                                                                   | `apps/api/src/webhooks/` (PR #188)                                   |
| Sync gate with real `current_stage`/`progress_pct` (D224)                                                                                                                 | `sync-gate.tsx`, `use-sync-status.ts`                                |
| Brief FE (all edge states) + brief-snapshot worker registered + real Haiku narrative w/ fallback                                                                          | `brief-screen.tsx`, `worker.ts:635-647`                              |
| Activity feed FE (filters, bulk+per-row undo, CSV, grouping, stats)                                                                                                       | `activity-screen.tsx`                                                |
| Followups API + FE list (worker unregistered — see U-WIRE)                                                                                                                | `followup.controller.ts:38-67`                                       |
| Mailbox connect-2nd/switch/disconnect/reconnect (header AccountMenu)                                                                                                      | `account-menu.tsx`                                                   |
| D206 dev login + dev-up.sh runtime                                                                                                                                        | `apps/api/src/auth`, `scripts/dev-up.sh`                             |
| 9 worker pipelines registered (initial/incremental sync, brief, score, label-action, unsub, undo-expiry, counter-recon, outbox)                                           | `worker.ts:541-1308`                                                 |
| Tier-capability metadata on action manifest (dead metadata — no consumer yet)                                                                                             | `manifest-entries.ts:126-144`                                        |

## 2. Violations / urgent fixes

- **§2.1 banned copy LIVE**: `sync-gate.tsx:327` renders "🔒 Bodies read: 0 — forever";
  locked in by `sync-gate.test.tsx:60,77`. Fix = mount shared `PrivacyBadge` (D228) +
  flip tests + add `check-microcopy.sh --rule=privacy-badge`. → **U05** (Wave 0).
- **Push pipeline dead at source**: no code calls `users.watch` — the prod-enabled
  webhook can never receive an event. → **U16**.
- **Latent fake-completion**: unreachable Protect tracer tail fabricating receipts
  (`senders-screen.tsx:938-955`); stale comments contradicting shipped endpoints. → **U09**.
- **IMPLEMENTATION-LOG drift**: 7 rows overstated (D34, D232, D228, D6, D38, D101,
  D211), 1 understated (D225), D52 missing shift-click. → **U06** (Wave 0).
- **Gate network is report-only**: `subagent-gate.yml` prints what WOULD run; no
  `ANTHROPIC_API_KEY` repo secret. CLAUDE.md §7 "must-pass" gates do not actually run
  in CI. → decision queue F8; local gate runs substitute during buildout.
- **Playwright does not exist** (no dep, no config) — the §8 "E2E pass" DONE-bar is
  currently unsatisfiable. → **U04** (Wave 0).

## 3. Build units

Effort: S=hours, M=1–2 agent-days, L=3–5, XL=week+. FG = founder-gated leg.
"Serial" = touches a serialization point (lands via the integration tail, §5).

### Wave 0 — foundations (parallel, disjoint files)

| Unit                 | What                                                                                                                                                                                                                                                                                                                             | Effort | Notes                                                                |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------- |
| **U01 schema**       | ONE consolidated Drizzle+Atlas PR: `billing_customers`, `subscriptions`, `subscription_events`, `users.billing_region`, `waitlist`, `account_deletion_requests`, `cron_runs`, `dead_letter_jobs`, `activity_log` rule-attribution, `sender_policies.snooze_until`, screener queue state, `users.onboarded_at` + `users.timezone` | M      | FG: founder merges (CI applies). Unblocks all schema-dependent units |
| **U02 entitlements** | Tier manifest in `packages/shared/src/entitlements/` per locked D19 ladder (Free $0/1 inbox/5 lifetime cleanup; Plus $9·$90/1; Pro $19·$190/2/30d-undo; Team/Enterprise rows; Founding Pro $129 promo). Price-ID fields (empty until U11 provisions). Tier-resolved undo-window resolver. Fix D77 misattribution comment         | M      | New files; zero serialization                                        |
| **U03 auth-split**   | Move `AuthProvider` from root `providers.tsx` into `(app)` group + onboarding; create `(marketing)` public route group; robots.ts/favicon skeleton                                                                                                                                                                               | S      | Hard prerequisite for ALL public pages; everything rebases on this   |
| **U04 e2e-harness**  | Playwright workspace + dev-login auth fixture + 3 golden specs (triage verb, sender policy, undo) + CI job                                                                                                                                                                                                                       | M      | Gives later agents a real e2e DONE-bar                               |
| **U05 banned-copy**  | Swap `sync-gate.tsx:327` TrustBadge → `PrivacyBadge`; flip tests/stories; add microcopy rule                                                                                                                                                                                                                                     | S      | §2.1 violation — lands first                                         |
| **U06 log-fix**      | Correct 9 drift rows; stop `(D38)` umbrella tagging                                                                                                                                                                                                                                                                              | S      | docs-only                                                            |

### Wave 1 — feature fan-out (parallel after Wave 0)

| Unit                     | What                                                                                                                                                                                                                                                                                                                                                         | Effort | Deps                           | FG                                                       |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ------------------------------ | -------------------------------------------------------- |
| **U07 landing**          | `/` landing per D134 10-section + D223 headline + trust strip (PrivacyBadge) + OG/sitemap + waitlist form FE                                                                                                                                                                                                                                                 | XL     | U03                            | —                                                        |
| **U08 legal**            | `/privacy` `/terms` `/refunds` content per D7/D228 posture                                                                                                                                                                                                                                                                                                   | M      | U03                            | F2 copy review                                           |
| **U09 senders-cleanup**  | Unsub secondary "also act on past emails" execution; status-aware unsub pill; sender-level `unsubscribe_method` derivation in incremental sync; delete Protect tracer tail; shift-click range select (D52); stale comments                                                                                                                                   | M      | —                              | —                                                        |
| **U10 pricing**          | `/pricing` 5-tier grid from manifest + Founding Pro + Team waitlist + Enterprise contact; waitlist API module (rate-limited, unauthed)                                                                                                                                                                                                                       | M      | U01 U02 U03                    | —                                                        |
| **U11 billing-be**       | `BillingProvider` interface + Paddle + Razorpay adapters; checkout session + manage endpoints; webhook controllers **in `apps/api/src/webhooks/`** (gate path) w/ full signature verification + dedup/replay protection; catalog-provisioning GH workflow (`workflow_dispatch`, prints price-IDs); contracts + error codes; vendor guardrail rows (ADR-0023) | XL     | U01 U02                        | F1 webhook secrets; F3                                   |
| **U12 entitle-enforce**  | `EntitlementGuard`: 402 + `FREE_CAP_REACHED` on actions (5-lifetime counting: per-sender, autopilot exempt-by-tier, no undo refund); inbox-limit on connect-mailbox; `tier` on `/api/auth/me`; downgrade semantics                                                                                                                                           | L      | U02, U11 schema                | —                                                        |
| **U13 billing-fe**       | Billing screen (plan card, checkout via Paddle overlay/Razorpay, cancel/change); upgrade modal; tier gates on Brief/Followups/Screener; triage empty-state CTA wiring                                                                                                                                                                                        | L      | U10 U11 U12                    | —                                                        |
| **U14 autopilot-be**     | Action consumer worker (Gmail mutation + undo_journal via label-action tx pattern, source='autopilot' attribution); approve-all/selected endpoints; dry-run preview endpoint (extract signal materializer); trigger wiring (sync-ready + score-completed); seeder-on-connect hook (registration itself → U-WIRE)                                             | L      | U01                            | —                                                        |
| **U15 autopilot-fe**     | Rules management UI (toggle/threshold/last-run); approve flow UI; grouping by rule; day-7 observe-window banner (no auto-promote); preview UI                                                                                                                                                                                                                | L      | U14                            | —                                                        |
| **U16 watch**            | `users.watch` after initial-sync ready + on reconnect; `WatchRenewalWorker` (6h, cron_runs); `users.stop` on disconnect + deletion purge                                                                                                                                                                                                                     | M      | U01                            | —                                                        |
| **U17 email**            | Notifications module + Resend client + plain-text typed templates; durable `email-send` worker (idempotent, suppression on bounce webhook); sync-complete + 24h-reminder triggers via outbox; email prefs toggle                                                                                                                                             | L      | U01                            | F4 DNS done ✅; real-send smoke via GH Action            |
| **U18 quiet**            | `quiet_state` GET/PUT + config UI + enforcement (defer autopilot mutations in window)                                                                                                                                                                                                                                                                        | L      | U14                            | —                                                        |
| **U19 snoozed**          | Later-review surface + `snooze_until` wake controls + wake sweep worker                                                                                                                                                                                                                                                                                      | L      | U01                            | —                                                        |
| **U20 screener**         | BE module (queue read/decide), soft quarantine via autopilot path, queue UI, badge count, Pro gate                                                                                                                                                                                                                                                           | XL     | U01 U12 U14                    | —                                                        |
| **U21 onboarding**       | Steps 1/2/4/5 (promise w/ PrivacyBadge, connect, preset pick, first-triage) + step machine + `onboarded_at`; strict sync gate redirect (layout.tsx leg → U-WIRE)                                                                                                                                                                                             | XL     | U01 U03                        | —                                                        |
| **U22 deletion**         | Persistence + controller + purge worker + sync pause + per-USER undo max (fix per-mailbox bug) + typed waiver; UI (typed confirm, grace banner → U-WIRE, cancel); deletion emails                                                                                                                                                                            | L      | U01 U16 U17                    | pre-approved ✅; F5 semantics confirm                    |
| **U23 settings**         | Real settings index; D34 skip-sheet persistence (zustand/persist + per-verb toggles card); privacy/data sub-page (PrivacyBadge + storage card + undo-retention CSV + DPDP export); mailboxes card; VIP list + stale comments                                                                                                                                 | L      | U22 endpoints for account card | export = privacy-adjacent, copy locked to D228 allowlist |
| **U24 followups-finish** | Dismiss button FE + D89 gate hook                                                                                                                                                                                                                                                                                                                            | S      | U12                            | worker reg → U-WIRE                                      |
| **U25 csp-seo**          | Nonce-based CSP middleware (D175) allowlisting PostHog/Sentry/Paddle/google avatars; API helmet headers; full SEO (sitemap entries, OG images)                                                                                                                                                                                                               | M      | U03 U07                        | F6 §9 review                                             |
| **U26 beta-gate**        | Invite allowlist on user-creation path (env-driven), waitlist fallback redirect                                                                                                                                                                                                                                                                              | S      | —                              | F7 confirm                                               |
| **U27 activity-attr**    | Rule-attribution read path + load-more infinite query                                                                                                                                                                                                                                                                                                        | S      | U01 U14                        | —                                                        |
| **U28 edge-inventory**   | D211 inventory: 6 → all 13 screens                                                                                                                                                                                                                                                                                                                           | S      | —                              | —                                                        |
| **U29 dead-letter**      | `dead_letter_jobs` write in BaseDeclutrWorker terminal + DeadLetterWorker (adminPolicy, Sentry alert)                                                                                                                                                                                                                                                        | M      | U01                            | —                                                        |

Every Wave-1 agent also: adds D159 observability events per taxonomy for its surface,
adds Zod contracts in `packages/shared/src/contracts/` for new endpoints, rate-limits
new unauthenticated endpoints, and updates `.env.example`.

### Wave 2 — integration tail (serial, single-threaded)

| Unit        | What                                                                                                                                                                                     |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **U-WIRE**  | ONE PR: register in `worker.ts` — autopilot-apply + consumer, followup-check (+6h cron), email-send, watch-renewal, deletion, snooze-wake, dead-letter; seeder hook; outbox router cases |
| **U-ENV**   | ONE PR: `deploy-cloud-run.yml` — all new env vars (RESEND/PADDLE/RAZORPAY/flags), API `min-instances=1` (full-replace pitfall)                                                           |
| **U-NAV**   | ONE PR: sidebar trim to built surfaces + `(app)/layout.tsx` merges (strict gate, grace banner, screener badge)                                                                           |
| **U-SMOKE** | Full-app smoke via dev-login: every nav destination, every transition, both mailboxes, edge states; report green/broken                                                                  |

## 4. Locked decisions applied (not re-litigated)

Billing in beta (Paddle + Razorpay) · D19 5-ladder via configurable manifest ·
deletion = 7d grace + immediate option · legal on `.com` · From `hello@send.declutrmail.com` ·
Autopilot approve-flow IS in scope (overrides in-code "excluded at V2" comments) ·
Free cap = **5 lifetime cleanup actions** (manifest) — supersedes the 25/day D77 display path.

## 5. Serialization points (one owner each)

`apps/api/src/worker.ts` (U-WIRE) · `packages/db` schema+migrations (U01, then frozen) ·
`packages/shared/src/shell/sidebar.tsx` + `(app)/layout.tsx` (U-NAV) ·
`deploy-cloud-run.yml` env (U-ENV) · `providers.tsx`/root layout (U03) ·
`senders-screen.tsx` (U09 owns) · `settings/page.tsx` (U23 owns) ·
`/api/auth/me` contract + `error-codes.ts` (U12 owns) ·
`outbox-consumer-router.ts` cases (U-WIRE merges).

## 6. Founder decision queue

| #   | Decision                                                                                                            | Recommendation / exact step                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | **Paddle + Razorpay webhook secrets** (mid-build)                                                                   | After U11 PR merges + deploys: Paddle dashboard → Developer Tools → Notifications → add destination `https://api.declutrmail.com/api/webhooks/paddle` → copy secret → `gh secret set PADDLE_WEBHOOK_SECRET`. Razorpay dashboard → Webhooks → add `https://api.declutrmail.com/api/webhooks/razorpay` (events: subscription.\*) → `gh secret set RAZORPAY_WEBHOOK_SECRET`. Then re-deploy |
| F2  | **Legal copy sign-off** (privacy matches D7/D228; refund policy)                                                    | Review U08 PR before merge                                                                                                                                                                                                                                                                                                                                                               |
| F3  | **Billing catalog provisioning**                                                                                    | I trigger `gh workflow run provision-billing-catalog.yml` (sandbox keys, additive) and write price-IDs into manifest — flag if you want to run it yourself                                                                                                                                                                                                                               |
| F4  | ~~Resend~~ DONE per ledger                                                                                          | —                                                                                                                                                                                                                                                                                                                                                                                        |
| F5  | **Immediate-delete semantics**                                                                                      | Built as: default = D232 `max(now+7d, latest_undo_expires_at)`; typed waiver "DELETE AND WAIVE UNDO" = purge ASAP (true immediate). Confirm or I flip to plan-pure (waiver only drops undo extension, keeps 7d floor)                                                                                                                                                                    |
| F6  | **CSP/security headers** (§9)                                                                                       | Review U25 PR before merge                                                                                                                                                                                                                                                                                                                                                               |
| F7  | **Beta invite gate**                                                                                                | Recommend YES (S): env-allowlist on signup, everyone else → waitlist. Without it "private beta" is open signup                                                                                                                                                                                                                                                                           |
| F8  | **Real CI gates**                                                                                                   | `gh secret set ANTHROPIC_API_KEY` to make §7 gates actually run; until then gates run locally per-PR during this buildout                                                                                                                                                                                                                                                                |
| F9  | **Plan-drift: CLAUDE.md §2.2** claims `verdict='screen'` enum; shipped enum stores `later` (D227)                   | CLAUDE.md needs founder edit                                                                                                                                                                                                                                                                                                                                                             |
| F10 | **DNS cutover** apex `declutrmail.com` Squarespace → Vercel                                                         | AFTER U07+U03 merge+deploy (today's placeholder is the less-bad option)                                                                                                                                                                                                                                                                                                                  |
| F11 | **Migration PR (U01) merge**                                                                                        | §9 prod migration — review schema PR first; CI applies on merge                                                                                                                                                                                                                                                                                                                          |
| F12 | **Brief email digest** deferred post-beta (not in locked scope; ships mail metadata to Resend = §9 privacy surface) | Confirm deferral                                                                                                                                                                                                                                                                                                                                                                         |

## 7. Cross-cutting invariants for every build agent

CLAUDE.md §2 guardrails · §8 smoke (dev-login, both mailboxes, every state, try to
break) · §10 no fake completion · D226 preview before mutation · K/A/U/L/D verbs ·
design freeze (`redesign` label for visual changes) · scope-change ⇒ reset scoped
cache · guard 4xx = designed state, never retry · Cloud Run env full-replace ·
billing-guardrails posture (ADR-0023) for new vendors · Gmail quota: unsub/autopilot/
watch consumers share the 15k units/min/user budget via the existing limiter.

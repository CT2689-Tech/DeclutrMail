# Billing and public Pricing implementation evidence — 2026-09-22

Final integrated results and browser evidence are recorded in the [delivery audit](implementation-delivery-audit-2026-09-22.md). The sections below retain the workstream’s intermediate checks and their historical limits.

Branch: `claude/product-simplification-ideas-5a8515`. Implements the concrete Billing/Pricing recommendations in `product-experience-review-2026-09-22.md` and `screen-review-billing-notes-2026-09-22.md`. This is this agent's implementation report, not overall goal completion or production payment certification. No live provider calls, mailbox changes, subscription mutations, deployment or commit was performed.

## Requirement → delivery → evidence

| Requirement                                   | Implementation                                                                                                                                                                                                                                                                                                               | Verification                                                                                                                                                                           |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Prioritize management before shopping         | `billing-screen.tsx`: current plan, coverage, healthy payment row and invoices precede plan picker. Past-due payment recovery appears before current-plan summary. Manage plan anchor complements the still-visible Review cancellation action; help/refund links remain available.                                          | New hierarchy test verifies invoices precede plan-change actions. Existing billing-history, disabled-billing and payment-lock cases pass.                                              |
| Explicit workspace / multi-inbox coverage     | `plan-coverage.tsx`: actual active connected count, manifest inbox allowance, cleanup allowance, over-limit explanation and included tool links; screen states workspace scope. No new request or stored metric.                                                                                                             | Coverage test verifies 1 of 5 and Daily Brief route; existing auth/entitlement data is reused.                                                                                         |
| Honest usage and value                        | Free's existing server remaining/reset presentation retained; coverage distinguishes allowance from usage. Unlimited cleanup is not a usage total. Sender cleanup counting and exempt Keep/Unarchive explained.                                                                                                              | Existing Free quota/reset cases retained and pass. No invented activity totals, time savings or financial ROI.                                                                         |
| Configured price versus renewal               | Current summary says Plan price, separate from Next renewal. Immediate change preview separately labels next billing date and configured plan price. `chargedPlanPrice` documentation corrected.                                                                                                                             | Current-summary regression assertion and exact provider-preview tests pass. No provider-confirmed renewal amount is claimed.                                                           |
| Healthy payment compression / recovery first  | `payment-method-card.tsx` uses a compact wrapping provider/action row with short explanation; support-only and past-due states retain expanded recovery treatment. No invented card details.                                                                                                                                 | Existing payment-method, unsupported-provider and pending-disable tests pass. Parent browser audit still required for visual quality.                                                  |
| Lifecycle clarity and access/history grouping | Existing cancellation, scheduled change, pause, refund, comp and non-backing truth distinctions preserved. Existing status slot remains above current access; receipts now precede resubscribe/shopping, including after cancellation. Added manifest consequences to cancellation/downgrade previews.                       | Full existing lifecycle suite passes. New ended/light and past-due/dark stories plus existing lifecycle fixtures available. No new inferred cancellation dates.                        |
| Inline comparison / consequences              | `plan-coverage.tsx` provides manifest-derived feature/capacity comparison and target-tier differences; `plan-picker.tsx` has keyboard-focusable scroll region and native disclosure. Downgrade to Free and cancellation also show consequences.                                                                              | New test asserts Pro→Plus loses Brief/Follow-ups and existing inboxes continue. Deferred downgrade remains enabled without an immediate quote.                                         |
| Fresh successful quote required               | `api/use-plan-change-preview.ts` validates response with shared schema and re-fetches on mount. `plan-picker.tsx` blocks confirmation during loading/error/missing amount, expires quote after 30 seconds and exposes refresh. Click handler independently rechecks freshness before mutation. No periodic provider polling. | Tests cover initial failure→retry→success, missing provider amount, expiry at click, exact charge/credit, and existing accepted/ambiguous/definitive outcomes and cross-tab mutexes.   |
| Invoice retrieval                             | `invoice-history.tsx`: local status and inclusive From/Through filters, loaded-result count, no-match state, clear filters, explicit truncation help link. Partial/omitted/provider errors stay independent of filtering.                                                                                                    | Test exercises status, date boundaries, empty result, clearing, loaded-history scope and older-history mailto. Existing download, partial, unknown-status and ended-access tests pass. |
| Consistent editorial surfaces                 | Summary, plans, preview panels and receipt/payment sections use thinner boundaries and smaller radii; page spacing reduced.                                                                                                                                                                                                  | Source/diff review complete; new narrow/dark/ended stories prepared. Actual visual/overflow review belongs to parent's browser audit.                                                  |
| Pricing plans visible sooner                  | `marketing/pricing/pricing.css`: introduction top spacing and headline size reduced; founding notice spacing reduced. Prices/claims/CTAs retained.                                                                                                                                                                           | Pricing tests pass. First-viewport visibility needs parent's exact viewport screenshot verification.                                                                                   |
| Pricing permissions continuity                | `marketing/pricing/cta.ts` routes unauthenticated visitors to `permissionEntryUrl(destination)`, retaining validated billing intent and attribution; authenticated visitors still go directly to app/Billing.                                                                                                                | Updated CTA tests pass. Helper owned by public agent; no duplicate parser.                                                                                                             |
| Currency-accurate annual savings              | Public Pricing's hardcoded badge replaced with shared manifest-derived helper. Billing helper now considers displayed provider currency and only advertises exact shared whole-month savings. USD remains two months; current INR points show Annual without the inaccurate exact claim.                                     | New currency regression test plus existing pricing-region and model tests pass. No price/catalog changes.                                                                              |

Paths in the table are under `apps/web/src/features/billing` unless otherwise identified.

## Verification performed

- `pnpm --filter @declutrmail/web test src/features/billing src/features/marketing/pricing`: **13 test files, 263 tests passed**, after final functional changes.
- `pnpm --filter @declutrmail/web typecheck`: **passed**, after final functional changes and concurrent owners' fixes.
- `git diff --check -- apps/web/src/features/billing apps/web/src/features/marketing/pricing`: passed.
- Reviewed own diff for mutation/state guard changes: existing provider selection, checkout reservation, mutex, polling, cancellation and webhook-authority paths preserved. Authorized change is the additional immediate-confirm quote guard.
- Applied Vercel React best-practices checklist: derived comparison/filter state, no new dependencies, no extra coverage request, bounded quote-expiry timer with cleanup, native labels/disclosure and focusable comparison scrolling.
- Added stories `ProNarrowDark`, `EndedNarrowLight`, `PastDueDesktopDark` in `billing-screen.stories.tsx`; the existing extensive lifecycle stories remain. Stories use synthetic data, not proof of provider behavior.

## Promise versus delivery / explicit remaining boundaries

1. **Provider-confirmed renewal totals and tax/discount lines:** not implemented. Review classified these as conditional adapter/service/contract expansion; current contract does not contain the data. Current UI explicitly presents a configured plan price and separate date. Immediate preview and invoice amounts retain their different provider-derived provenance.
2. **Complete invoice pagination, invoice numbers/due dates/billing periods/refund adjustment classifications:** not implemented. Local filters explicitly cover loaded rows; truncation remains visible with older-history support. No false all-history search claim.
3. **Real payment verification:** no financial action performed. The new quote policy is covered with mocked provider responses; sandbox integration/rehearsal remains required before treating it as payment-path certification. Do not use live billing for exploratory tests.
4. **Browser verification:** parent owns current authenticated/local visual audit. This agent has not claimed screenshot verification of every lifecycle, theme or viewport. Billing stories and code are ready for that audit.
5. **Collection management remains available:** plan shopping is lower on the page, not removed; cancellation remains a visible preview action. Feature consequences come from capability manifest, not invented data-deletion/disconnection policy.
6. **No request for card metadata or a second billing architecture:** provider-managed payment update path and current adapter stay intact. No new stored card details.

## Files

New: `apps/web/src/features/billing/plan-coverage.tsx` and this evidence note.

Modified billing: `api/use-plan-change-preview.ts`, `billing-model.ts`, `billing-model.test.ts`, `billing-screen.tsx`, `billing-screen.test.tsx`, `billing-screen.stories.tsx`, `cancel-modal.tsx`, `invoice-history.tsx`, `invoice-history.test.tsx`, `payment-method-card.tsx`, `plan-picker.tsx`.

Modified public Pricing: `cta.ts`, `cta.test.ts`, `pricing-screen.tsx`, `pricing.css`. Shared shell/tokens, API architecture and unrelated public pages not edited by this implementation task.

## Parent browser follow-up

Parent browser audit at 1280px dark confirmed management order, actual 2-of-5 inbox coverage and included feature links. Parent found native visited lavender on new coverage links. Fixed with local `billing-links.module.css`: explicit primary token for normal/visited, consistent underline offset, hover thickness, and visible keyboard focus. Applied to coverage, management/help, comparison, and invoice retrieval links. Invoice date/status controls now use wrapping grid labels, zero minimum width and border-box full-width controls for narrow layouts. Follow-up targeted Billing/invoice tests: **2 files / 118 tests passed**. Parent owns screenshot re-verification.

## Delayed-response regression audit

Added controlled fetch-promise tests for a late annual quote after switching to monthly and for closing/reopening while a quote is outstanding. The first was already isolated correctly by target/cycle. The second exposed query deduplication reusing the closed preview's outstanding request. The quote query now also includes a React mount ID; reopened previews own a fresh request. Obsolete responses cannot enable current confirmation or submit a change. Full Billing/Pricing suite after this fix: **13 files / 265 tests passed**.

Storybook global theme comes from `apps/web/.storybook/preview.ts:14` (document `data-theme`) and its `theme` global/toolbar. On the running Storybook origin use `/iframe.html?id=features-billing-billingscreen--pro-subscriber&viewMode=story&globals=theme:dark`; change to `theme:light` for light. Use real browser viewport sizes 390 and 1280: no viewport addon is configured. Other lifecycle suffixes: `free-tier`, `past-due`, `subscription-ended`, `cancel-scheduled`, `downgrade-scheduled`, `downgrade-confirming`, `downgrade-restoring`, `paused`, `refund-pending`, `refund-unconfirmed`, `payment-processing`, `payment-processing-slow`, `payment-unconfirmed`. Invoice fixture: `billing-invoices-payment-method--with-invoices`.

Added `features-billing-billingscreen--upgrade-quote-review`: opens the upgrade preview with a synthetic exact quote and intercepts **all** network requests while mounted, rejecting real change/payment actions. This fixture is safe for preview/retry inspection; it is not provider integration proof. General legacy seeded stories do not uniformly isolate mutations—inspect presentation only, and do not activate payment controls. Use fresh iframe navigation after legacy `billing-unknown`, `billing-disabled`, or invoice failed-read stories, which replace global fetch without cleanup. Source-supported routes/theme parameters are handed to parent for actual browser verification.

## Full-context unconfirmed payment review

Reworked `PaymentProcessingNotice` presentation into a status heading, **What we know**, **What happens next**, help, and labeled latest provider check. Removed the long reassurance paragraph. Checkout completion is attributed to the checkout report, not asserted as server-confirmed payment receipt; a `checkout_intent` explicitly says payment completion remains unknown. Other ambiguous/accepted/resume branches retain distinct evidence. Provider checks, duplicate-charge lock, release eligibility and two-step manual release handlers are unchanged.

Added full-screen synthetic story `features-billing-billingscreen--payment-unconfirmed-full-page`. It seeds the real `PendingCheckout` model in an isolated Storybook workspace, 16 minutes old, with `checkout_intent`; all fetches are intercepted and mutations rejected. Reconciliation answers provider unavailable so the recovery/help and guarded manual-release path can be inspected. Storage/fetch are restored on unmount. The story renders the whole Billing screen (heading, current access, coverage and locked plans), not just a notice. Use global `theme:light` / `theme:dark` and 390 / 1280 browser viewport widths. Parent owns visual verification.

Updated semantic tests cover full-page heading, knowledge/next-step labels, support destination, ambiguous checkout/change truth and existing manual-release protections. This closes the notice-hierarchy/full-context-preview implementation gap; provider integration certification is still outside this synthetic verification.

Final notice/full-page-fixture verification: **9 Billing test files / 220 tests passed**, web typecheck passed, diff check passed. Pricing code was unchanged in this follow-up.

## Pending-payment fact consistency

Parent's full-page browser review found the Free card's “no card on file” reassurance contradicted an unknown checkout outcome. `CurrentPlanCard` now suppresses that entire reassurance while either a local payment lock or server pending-checkout signal exists; known current entitlement and remaining cleanup allowance remain visible. Regression assertions cover both unknown checkout and old completed-checkout waits, including continued checkout lock. **100 BillingScreen tests passed**; no release/payment behavior changed.

## Final integrated verification and narrow recovery follow-up

- Integrated full web run: **236 files, 2,950 tests; 235 files / 2,949 tests passed, one public HowItWorks test failed** because a page-wide text query became ambiguous. Public owner scoped its assertions to the intended section. Follow-up run of BillingScreen plus HowItWorks: **2 files / 107 tests passed**, including all 100 BillingScreen cases and the 7 corrected public cases. This distinguishes the original full-run result from the focused remediation; no claim that the full suite was rerun after the fix.
- Repository lint exited 0: **0 errors, 7 warnings**, all unused ESLint-disable directives in unchanged API/worker/eval files. Sole additional exclusion: pre-existing `.playwright-mcp/**`; normal repository-configured generated/dependency exclusions remain. The web-test log also contains a Happy DOM fetch-abort during teardown, not another failed assertion.
- Integrated web typecheck passed. Logs: `/tmp/declutrmail-experience-final-web-tests.log`, `...-lint.log`, `...-typecheck.log`; focused remediation logs: `...-followup-tests.log`, `...-followup-lint.log`, `...-followup-typecheck.log`.
- Parent's 390px browser review found a 327px recovery button inside a 292px notice area. All buttons within `PaymentProcessingNotice` now wrap text, use max-width 100% / min-width 0, and grow vertically from a 36px minimum. This includes both manual-release stages and accepted-change retry. Shared button defaults, labels and handlers are unchanged. Focused file lint and diff checks pass; parent owns rebuilt browser measurement.

## Final full-suite rerun and initial-bundle refinement

The final full web rerun supersedes the earlier failed run: **236 files / 2,956 tests passed**, with no teardown AbortError this time. Exact log: `/tmp/declutrmail-experience-final-web-tests.log`.

After that run, parent build measured Billing/Pricing initial bundles slightly above their unchanged budgets. Moved annual-savings helpers into the already-shared pure public pricing model (Billing keeps compatibility re-exports), removing Pricing's dependency on the Billing lifecycle model. Extracted the unchanged optional plan-change review and quote hook into `change-plan-panel.tsx`, loaded only after selection with a visible loading status. Confirmation remains unavailable until the panel and valid quote are ready; tests await that real asynchronous boundary. Post-refinement validation: **13 Billing/Pricing test files / 265 tests passed**, web typecheck passed, focused lint passed. No build run by this agent; parent measures final bundle sizes and conducts rebuilt-browser checks. Budgets were not raised.

A further bounded refinement extracts the manifest comparison into `billing-comparison.tsx`, dynamically loaded only while its native disclosure is expanded. The summary, plan coverage and change consequences remain immediately available; the table retains its named, focusable scroll region and gains a loading status. Added a regression test for opening/closing the table and its actual manifest inbox row. **101 BillingScreen tests passed**, web typecheck and focused lint passed; logs `/tmp/billing-comparison-{tests,typecheck,lint}.log`. Parent verified the table and quote panel in the 390px light preview. Final build measurement remains parent-owned.

After the next build still measured Billing at 186.4 KiB against its unchanged 186 KiB budget, extracted the existing new-checkout confirmation and provider selector together into `checkout-confirm-panel.tsx`. It loads after plan selection with a status placeholder; all parent state, mutation handlers, mutex and price guards are unchanged. Two existing tests now await the asynchronous panel mount. **13 Billing/Pricing files / 266 tests passed**, web typecheck, focused lint and diff checks passed. Logs `/tmp/billing-checkout-split-{tests,typecheck,lint}.log`; final size is for parent build to measure.

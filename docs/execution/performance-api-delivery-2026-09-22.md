# API performance delivery — 2026-09-22

Source work on `claude/product-simplification-ideas-5a8515`. No live database/provider calls, load tests, deploys or schema changes by this agent. Query mechanisms are verified locally; no production latency improvement is claimed.

| Requirement                                 | Implementation                                                                                                                                                                                                                                                                                                                                                                                                 | Meaningful verification                                                                                                                                                                                                                                                 |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Parallel Triage enrichment                  | `triage.read-service.ts`: outbound-index status, per-sender engagement aggregates and actionable-inbox aggregates start together after the bounded sender page; original predicates unchanged.                                                                                                                                                                                                                 | Deferred outbound promise test verifies both aggregate reads start before it resolves; existing PGlite projection/tenant/outbound/shield tests retained.                                                                                                                |
| One auth sync-state read                    | `SyncService.getMailboxHealth` returns readiness and reconnect status from one bulk projection; `/auth/me` uses it. Existing readiness/reconnect APIs remain compatible.                                                                                                                                                                                                                                       | Bulk test asserts one SELECT, mixed ready/failed/reconnected states, omitted missing mailbox and empty-list short circuit. Existing reconnect-recency and auth-envelope tests pass.                                                                                     |
| Reduce Autopilot intermediate join          | Digest query filters matches to pending OR recent. Separate derived aggregation counts matches; distinct recent rule/sender pairs join current inbound INBOX messages once each. Final derived-table join is one SQL statement, preserving a common snapshot.                                                                                                                                                  | PGlite test mixes duplicate recent/history matches, old pending, old resolved and inbox/archive messages. Existing tenant isolation, old inbox backlog and rebuilt-sender-index tests retained.                                                                         |
| Bounded Activity hydration and exact totals | SQL selects latest unresolved attempt for an eligible root before page LIMIT, sender/date/verb/outcome/cursor filtering. Any successful forward recovery suppresses the lineage. Tuple ordering mirrors recovery attempt, creation time, then ID. Hydrates at most limit+1 attempts; failure totals run SQL counts using the same root/current scope, independent of page/source/verb/outcome chips as before. | Added PGlite test verifies pre-limit sender/outcome filtering, tied timestamp cursor traversal, at-most-limit+1 hydration and totals beyond page. Existing recovery success, later-attempt precedence, date, sender wildcard, reverted-state and export tests retained. |
| Followup keyset/repeated policy work        | Replaces OFFSET with `(sentAt,id)` keyset; derives each row sender hash once; request-local policy map queries each unique recipient key once. Keeps 100 returned/1,000 scanned cap and metadata-only hashing in TS.                                                                                                                                                                                           | PGlite fixture: 230 identical timestamps, 220 excluded then 10 eligible; returns all 10 in order using 3 page reads + 2 policy reads. Subsequent request sees policy changed to Keep (no cross-request cache).                                                          |

All five optimized paths use parent-owned `measureRequestOperation` with fixed operation names (`triage.enrichment`, `auth.sync-state`, `autopilot.observe`, `activity.lineages`, `followups.scan`). No query text, parameters or mailbox content is added to telemetry. The `activity.lineages` operation measures bounded lineage selection/hydration; `activity.stats` measures the complete statistics reads and projection.

## Verification

- Focused services + auth controller + sync suites: **12 files / 226 tests passed**, log `/tmp/perf-api-tests.log`. Includes five new meaningful tests; initial existing suite had 221 passing tests.
- API typecheck passed: `/tmp/perf-api-typecheck.log`.
- Focused lint passed after removing an obsolete Activity helper: `/tmp/perf-api-lint.log`.
- Broader six-feature test run: **28 files / 469 tests passed** (96.26s): `/tmp/perf-api-feature-tests.log`.

## Boundaries and index evaluation

- Followup policy reuse is request-local; a policy changed concurrently during that request may be observed on the next request. There is no TTL or cross-request entitlement/policy cache.
- Activity now bounds application hydration/transfer. Database CPU and pool wait still require representative query-plan/runtime measurements; no assertion of constant database work is made.
- Parent owns index review. Existing Followup partial awaiting index is `(mailbox_account_id, sent_at)`; evaluate adding `id` for tied keyset traversal. Activity latest-attempt anti-join anchors on `root_action_id`; an existing partial unique index on `(root_action_id, recovery_attempt)` already supports this prefix. Evaluate the current-attempt join and outcome-time sort with representative plans before proposing any additional index.
- Existing session revocation checks, coalesced session touch writes, Paddle timeouts, bounded export iteration, and deployment warm-instance settings were not replaced.

## Activity statistics follow-up

Independent review found an inherited counting bug: repeated qualifying actions for the same sender multiplied that sender's last-90-day message volume. `aggregateStats` now selects distinct qualifying sender keys before the message join. Original action counts remain event counts; reverted actions, action window bounds, sender filtering, message-age cutoff, zero/null projection and mailbox scope remain unchanged.

Grouped action counts, impact volume and failed-execution totals now run concurrently inside `activity.stats` timing. When window bounds are both null, the window and all-time outputs share one aggregate promise instead of issuing identical reads twice.

Added three regressions: real PGlite repeated-sender actions with a >90-day message, a blocked grouped-query fixture proving the other reads start before it resolves, and an all-time request asserting a single aggregate invocation. Focused Activity verification: **64 tests passed**, `/tmp/perf-api-activity-stats-tests.log`; focused lint and diff checks passed. API typecheck found only two errors in the concurrently edited Gmail test fixture (`FetchInit.signal`), reported to that owner; no Activity type errors. Parent owns the integrated recheck after that fix.

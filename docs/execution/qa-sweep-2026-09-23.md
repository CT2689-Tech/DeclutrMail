# Coordinated launch QA — 2026-09-23

Candidate: `claude/product-simplification-ideas-5a8515`. An intermediate QA coordinator assigned exclusive journeys to Clean up and Catch up reviewers, reconciled overlap, and requested retests after fixes. This report covers the local release candidate, not production launch approval. The two connected Gmail accounts were used for safe read and preview checks; the guided demo and isolated browser stack used fictional or synthetic data.

| Journey                      | Evidence                                                                                                                                                           | Result                                                                                                                                                                                                                                                                                     |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Senders and Triage           | Both connected accounts: search, domain/Protected filters, groups, right-hand inspector, selection, bulk previews, Protected exclusion/override, Focus/List.       | Pass. No new Gmail action in this pass.                                                                                                                                                                                                                                                    |
| Screener                     | Live filters, sort, decision previews, Delete reach and age window.                                                                                                | Pass after fix: zero-count Delete cannot confirm; selecting Any age showed the two currently matching Inbox emails. Canceled.                                                                                                                                                              |
| Account switching            | Two tabs with a selected sender list, open bulk preview, and full Sender Detail preview.                                                                           | Pass after fix: the other tab clears the preview and selection, reloads counts and rows, rejects a sender outside the selected account, and restores the original detail without a stale dialog on switch-back.                                                                            |
| Brief, Follow-ups, Later     | Live date and account isolation, readable generated note, safe Archive preview, follow-up groups/links, Later empty state.                                         | Pass. Previews canceled. Prior controlled Later/return and Archive/Undo remain documented in the [launch sprint](launch-sprint-2026-09-23.md).                                                                                                                                             |
| Autopilot                    | Live five-rule page, status filters, pending queue, preview and confirmation copy; API/worker safety tests.                                                        | Pass after fix: low-engagement Archive and new-sender Later require individual review, including legacy Active rows. The low-engagement live rule remained disabled; preview showed a matching sender with zero current Inbox mail and offered no Act now. No rule activation or approval. |
| Workspace and administration | Home totals/navigation, Activity filters and Undo labels, Settings/Protected/Privacy/Help, billing prices/invoices/cancellation preview, Admin Security read-only. | Pass. No settings, billing, or admin writes.                                                                                                                                                                                                                                               |
| Public site and demo         | Home, pricing, comparison, how-it-works, methodology, security, beta, sign-in, contact; demo Senders/Triage/tour/bulk/history previews.                            | Pass. Guided demo now subtracts previously archived fictional mail: after four Amazon senders/420 emails were archived in the demo, one remaining sender/156 Inbox emails was ready for rule review. Category badges that the connected product does not show were removed.                |
| Phone viewport               | 375×812 on home, pricing, comparison, and demo.                                                                                                                    | Pass after shared-header fix: no horizontal overflow, visible Menu and accessible theme switch.                                                                                                                                                                                            |

The same day's earlier controlled Gmail tests established Archive → Undo, Later → return, and Delete → Undo on one promotional message, with Gmail labels checked. This coordinated pass concentrated on broad coverage and safe previews, and made no additional Gmail mutation.

Open product-quality follow-ups: Triage can lead with a strong Archive recommendation for a sender with zero current Inbox mail; its preview safely disables confirmation. The broad new-sender preset can generate a large review queue, while pending suggestions lack search, filter, and sort. Both are worth addressing in a focused post-launch iteration; neither currently authorizes unattended movement through the review-only presets.

The current visual pass could not complete Quiet Hours or the long-tail public footer pages during concurrent local builds. Quiet Hours was toggled and restored Off in the prior connected smoke, and a prior 27-route HTTP scan returned the public pages successfully. Mark these current visual checks unverified, not passed.

The isolated synthetic browser stack passed ten selected journeys: Brief, Follow-ups dismissal, Protected Settings, sender protection, three sender-search cases, Triage Keep and preview cancellation, and two public acquisition paths through the demo and sign-in. The public test had still targeted the retired tutorial and its old Undo label; it now exercises the actual Senders selection, bulk Archive preview, and Activity Undo. It checks the first-touch referral through the API's authoritative cookie or the decorated OAuth URL. Cold Next compilation caused initial navigation timeouts in some routes; reruns passed after compilation, and the public journey now permits the dev server's full navigation window. These tests used synthetic accounts and never touched Gmail.

Focused checks passed: Autopilot API **70/70**, Autopilot Web **68/68**, Autopilot worker **67/67** across four separately run files, public/product UI **173/173** after updating an outdated copy assertion, and shared action-safety **5/5**. The first combined worker run was stopped after ten CPU-bound minutes without a result; file-level runs isolated an outdated completed-unsubscribe test fixture, corrected it, and passed all four files. The two new low-engagement worker safety regressions also passed by name. Lint passed with seven existing warnings outside the touched files; all workspace typechecks passed.

Still outside this local smoke: fresh Google OAuth/connect and consent review, actual unsubscribe delivery, Paddle sandbox purchase/webhook/refund, live billing changes, account disconnect/deletion, bulk Gmail moves, outbound commercial email, production deployment, and authenticated monitoring dashboards. Commercial email remains disabled by the founder's launch choice. Complete the external release gates in the [launch sprint](launch-sprint-2026-09-23.md) before public promotion.

## Connected-account follow-up smoke

The web, API, and worker processes were running from this candidate branch. API health, readiness, and worker readiness each returned 200; both connected Gmail accounts remained available after the machine restart. The public `/home` and root routes returned normally after local development compilation.

The primary account passed a second controlled Gmail Archive → Undo round trip on exactly one unprotected promotional Inbox message. The product preview and Gmail exact-sender Inbox search both showed one eligible message. After Archive, the product showed “Archived 1 email,” the sender Inbox count fell from one to zero, the message left Gmail Inbox, and Activity showed the completed operation. Undo restored the product count and the same Gmail conversation to Inbox. This is evidence for the single-message path on the primary account; it is not a bulk-action or production-worker load test.

The secondary account showed 12 active senders, 40 across all statuses, one Screener item, and 12 Triage items. Its clearly promotional candidate had two Inbox messages; the available age scopes selected two or zero. The visible one-message candidates were transactional, terms, or renewal mail. The reviewer canceled the preview and did not move secondary-account mail. Account switching, account-scoped Senders/Screener/Triage reads, and cross-account sender rejection were checked without mutation.

Public pages were opened in a browser beyond the prior route scan: FAQ (including an expanded action-definition answer), Cookies, Help, Contact, Refunds, How-to, Answers, Blog and a blog article, Privacy, Security, and Terms rendered without an application error. Help’s Undo question implied every action was reversible while its shared answer began “No”; the question now accurately asks whether every action can be undone. A wrong-account sender detail had a “Back to Senders” button that used browser history and could land on Activity; it now targets `/senders` directly, confirmed in the secondary-account browser retest. The Help content checks passed 65/65, the Sender Detail checks passed 50/50, and the changed files passed lint, formatting, and web typecheck.

In the secondary connected account, Brief showed two FYI items. The generated note expanded to a bullet with a limited-details caveat, and its Gmail links specified the secondary account. Follow-ups was empty and explained its 60-day window and approximate six-hour scan interval. Later was empty and displayed the Los Angeles return timezone. Autopilot showed five disabled rules, no pending/Watch/Acting work, and a review-only low-engagement dry run with zero ready actions, zero of 38 matched, and zero Protected skips. No rule was activated or approved.

Secondary Activity showed no outcomes in its 30-day default view. The all-time view showed historical manual and Autopilot rows with Gmail links pinned to that account. The Later filter narrowed the rows and URL to `?verb=later`. The primary account's controlled Archive action was absent from secondary Activity, supporting account isolation.

Secondary Quiet showed both inboxes Off with a 22:00–07:00 local window and the secondary account marked Active. Settings showed both connections Ready, three preview switches Off, sync/reminder emails disabled, and the weekly receipt Off. Owner-only Admin Security rendered its paginated filtered table; visible OAuth refresh failures were historical September 19 informational events while both connections currently reported Ready. These were read-only checks.

Billing rendered an active Pro annual plan, two of five inbox slots, a July 31, 2027 renewal, and two paid invoices. It also showed a persistent “Checkout started” processing lock that disables billing changes and payment-method updates. The provider check first reported payment in progress, then on refresh said the provider could not be reached; the page offered Check again and a two-step user assertion for resuming checkout. The same lock remained after switching from the secondary to primary mailbox, as expected for workspace billing. The reviewer did not assert no charge, release the lock, or change billing. Source review indicates an existing workspace-scoped browser lock can persist until the target tier/cycle arrives or the user explicitly releases it. The exact origin and payment outcome remain unverified; this workspace's billing state needs manual reconciliation before calling its billing journey ready.

The first-step billing recovery button previously said “No charge went through” even when the provider could not be reached. Its label now says “Review before resuming checkout”; the existing second step still asks the user to check for a charge and warns about duplicate payment. Billing screen tests passed 102/102, with lint, formatting, and web typecheck passing. A final connected-browser check returned “payment still in progress,” which intentionally hides that release button, so the new label was verified by source and tests rather than that live state. The lock and subscription data were not changed.

After switching to the primary mailbox, Brief showed six Review, four FYI, and 76 Noise items for the latest September 22 edition, versus two FYI and no Review/Noise items in the secondary. Primary Autopilot showed five rules, 37 pending suggestions, one Watch, and zero Acting; secondary showed five disabled rules and zero pending/Watch/Acting. The different counts and account-specific Gmail links support isolation, while Billing correctly stayed workspace-level. No suggestion or rule was approved.

Primary Activity all-time totals were Archive 19, Delete 5, Unsubscribe 6, Later 4, and Keep 20, materially different from secondary totals of 5, 0, 9, 67, and 6. Today's controlled QA actions were marked Undone, primary Gmail links specified the primary account, and no secondary rows appeared in the primary view.

For a returning user, `/onboarding` briefly showed “Opening DeclutrMail…” and redirected to authenticated `/home` with the primary mailbox selected; it did not restart account connection.

Authenticated Home fit a 375-pixel viewport with its navigation menu accessible. The mobile Catch up route did not finish rendering during a hot development rebuild, so its mobile layout remains unverified in this follow-up; the desktop browser was restored. Local development cold compilation made first navigation to some pages slow; warm requests returned quickly. This observation does not establish production latency.

The active account was left on the primary mailbox. No unsubscribe delivery, bulk Gmail action, rule activation, billing/settings/admin write, account deletion or disconnect, or fresh Google OAuth was attempted in this follow-up. The secondary mailbox's live mutation and the unresolved workspace checkout outcome remain explicit open checks.

## Additional connected-account and billing smoke pass

The primary mailbox's promotional Cinemark sender was inspected for a one-click unsubscribe. Its indexed sender URL matched the newest message's URL, the sender was not Protected, and the preview selected **Leave historical email alone**. With the ordinary send switch off, the initial confirmation correctly refused before creating an action or changing Gmail. Sender Detail now explains this disabled state as “nothing was sent,” matching Senders and Triage. For the user-authorized external smoke, an ephemeral development-only gate matched this exact mailbox, sender, one-click URL digest, and one-time execution key at both API and worker while the global send switch stayed off. The request created exactly one intent and action. The worker made one POST; the provider returned HTTP 200, and the recorded outcome was `endpoint_accepted`. The indexed and live Gmail checks both showed the same one Cinemark conversation still in Inbox, as requested. No historical mail was archived or deleted, and the send was not retried. Endpoint acceptance does not prove future messages have stopped; that requires observing later mail. The normal API/worker processes were restored, the ephemeral scope was deleted, health/readiness checks returned 200, and the temporary gate was removed from the launch source after the smoke.

At a 375×812 viewport, connected Brief, Follow-ups, Later, Activity filters, Settings, the workspace menu, and Autopilot summary and rule cards had no horizontal overflow. Pending Autopilot suggestion rows were too compressed; they now stack the sender, reason, and Skip action on narrow screens. The agent rechecked the row at 375 pixels and the focused Autopilot tests passed (80/80), with typecheck, lint, and formatting clean.

Billing was exercised on isolated synthetic **local** accounts, never on the connected owner's subscription. The local API had sandbox Paddle credentials, a test Razorpay key, and all five configured catalog points on each provider matched the provider's test catalog. The development database identity guard confirmed the local cluster was distinct from production before inspection. A Paddle sandbox Plus monthly checkout completed for $9 with a test card. The first webhook deliveries failed while the temporary tunnel had exited; after restoring a persistent sandbox tunnel, Paddle retries delivered the events and the local workspace became Plus. Billing showed the correct price, renewal date, and paid invoice. The cancellation preview and scheduled cancellation matched Paddle and the database; **Keep my subscription** removed the scheduled change. A second cancellation was scheduled before a full sandbox refund. The refund was initially pending approval, then approved; the provider-truth billing worker moved the synthetic workspace back to Free, canceled the subscription, and Billing showed “Your Plus subscription ended.” No real charge occurred.

A fresh authenticated browser tab for the connected owner's workspace later showed its existing Pro annual plan, July 2027 renewal, payment-method action, and available plan changes without the earlier checkout-processing lock. The guarded local database also had no pending checkout for that workspace. The previous lock's origin was not determined, but it is not present in this final readback; no billing write was made to the owner's workspace.

The approved Paddle `adjustment.updated` event had arrived while the old adapter ignored it, leaving Billing on “refund is being processed” until the worker's later provider check. The adapter now maps an approved full refund to its existing guarded settlement path and a rejected full refund to the existing reversal path. Pending, partial, unlinked, and unrelated adjustments remain inert. Focused billing tests passed (113/113), including Plus-to-Free settlement and replay deduplication, plus API typecheck, lint, and formatting. The provider-truth worker remains the fallback if an update event is missing.

Razorpay **Test Mode** offered the India Plus monthly price of ₹749 and opened the correct subscription checkout. A synthetic contact and official subscription test card reached the card step, but the checkout did not authorize the payment in automated testing; the provider remained `created` with zero paid charges. The test subscription was canceled through the test API, the local pending-checkout claim was released through the product API after provider verification, and the synthetic account returned to an unlocked Free Billing screen. An end-to-end successful Razorpay charge and webhook grant remain unverified. The Razorpay-hosted page displayed a personal merchant name instead of DeclutrMail, so the merchant's checkout branding needs correction in the Razorpay account before launch. This observation is from the sandbox surface and should be checked again against the eventual live account.

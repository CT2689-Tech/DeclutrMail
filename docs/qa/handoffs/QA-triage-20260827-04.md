# Handoff — QA-triage-20260827-04 (Tier 1 · billing)

**Dispatched** 2026-08-27 · Codex task `task-mtckxmm4-9r9pb9` · approved on its
own, never bundled, per CLAUDE.md §2.0.

> ⚠️ **This brief was never delivered.** It was written after dispatch and left
> uncommitted, so it did not exist in the worktree Codex ran from. What the task
> actually received was an inline argument compressed by the runtime's size cap
> (~230 lines → 28 for the P1 group). The worktree has since been auto-removed.
>
> Treat any returned PR as built from a partial brief: review it against **this
> file**, not against what the task got. To use this brief properly it must be
> committed _before_ the next dispatch — see `.claude/commands/ct-qa.md` §8.

- **Status** — latent, not currently firing. A live tripwire with no alarm.
- **Cause** — `apps/api/src/common/entitlements/entitlements.service.ts:315` gates
  on `if (used + unitsNeeded > limit)`. `used` comes from a raw `sql<number>` at
  ~:188 and is returned via `?? 0` at ~:212 with **no `Number()` coercion**. It is
  correct today for exactly one reason: the fragment ends in `::int` (OID 23,
  which postgres.js parses). A bare `COUNT`/`SUM` returns OID 20 (bigint), for
  which postgres.js registers **no** default parser.
- **Failure if the cast is ever dropped** — `used` becomes a string, `"5" + 1`
  evaluates to `"51"`, `"51" > 50` is true, and a Free user is told
  `FREE_CAP_REACHED` at 6 of 50 actions. A paywall that inverts silently.
- **Why it would ship green** — `entitlements.service.spec.ts` imports
  `drizzle-orm/pglite`. PGlite's decode path is not the postgres.js path
  production runs, so the suite asserts a behaviour production does not use. This
  is the same blind spot that let QA-triage-20260827-02 live eight weeks.
- **Fix, both halves (founder approved both)** — (1) add explicit `Number(...)`
  at the consumer so the cast stops being load-bearing; keep the `::int` as well.
  (2) add a test exercising the **real postgres.js** decode path, following the
  repo's existing testcontainers pattern (D182/D183/D206). If a postgres.js-backed
  test genuinely cannot land in this PR, do **not** stub it — say so plainly and
  ship the coercion (CLAUDE.md §10, no fake completion).
- **Negative control, required** — with the test in place, remove the `::int`,
  confirm RED, restore. State it in the PR body.
- **Boundaries** — `apps/api/src/common/entitlements/**` and its specs only; a
  concurrent task owns `apps/api/src/triage/**` and `apps/web/src/features/triage/**`.
  Do **not** change any tier's limits or `pricing.config.ts` — that manifest is
  executable truth and entitlement values are a founder decision. Do not touch
  payment webhooks, subscription state, or provider catalog ids. The invariant
  test that no tier grants `autopilot` without `quiet` must still pass.
- **If the arithmetic turns out to be already broken in some live path** — stop
  and report. That is a Tier 1 escalation, not a patch.
